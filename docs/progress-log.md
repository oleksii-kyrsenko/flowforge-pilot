# FlowForge — Progress log

Append at the top; same entry format as before; see the MAINTENANCE RULE in CLAUDE.md.

- 2026-06-21 — **Engine v3.52→v3.53 — read-only allow-list + `addComment`=A hook + `format:check` (§9/§9.1):** added `permissions.allow` to `.claude/settings.json` — Figma reads (`get_metadata`/`get_design_context`/`get_variable_defs`/`get_screenshot`/`download_assets`) + npm `lint`/`typecheck`/`test:run`/`format:check` — **read-only only**; writes/installs/`createJiraIssue`/transitions are NOT allow-listed. Extended the Jira PreToolUse hook + matcher to auto-allow `addCommentToJiraIssue` (a NEW comment = **class A**, add-only/append-only per §9.1, history preserved) while `createJiraIssue` + edit-existing stay gated (**class B**); the hook's safe-failure invariant is intact (only explicit branches `allow`, else `ask`, always `exit 0`). Created the `format:check` script (`prettier --check .`) the §9 allow-list references (the former `format --check` phantom; `format` = `prettier --write .` stays a write, not allow-listed). §11 updated to match the hook (hook-description gains `addCommentToJiraIssue`; 51/`createJiraIssue`/edit-existing gated) + a pointer line to the allow-list (`settings.json` = home, not enumerated in the law). Source: spec-first (FLOWFORGE_ENGINE_SPEC §9/§9.1). **Engine edit → the freeze-counter does NOT start.**
- 2026-06-21 — **Engine v3.51→v3.52 — version-bump obligation into the MAINTENANCE RULE (engine-PR hygiene enforcement, closes T23-a):** added one line to the CLAUDE.md MAINTENANCE RULE — a change to the law itself (this file or any engine file: `.claude/`, `.mcp.json`, engine config) additionally bumps the Version line; one law-changing PR = one bump. This ENFORCES the version-bump rule on the running agent (it was previously only spec-side intent in FLOWFORGE_ENGINE_SPEC §14.5), alongside the progress-log obligation that hard rule 7 already mandates. Rationale: the chat-context mirror is tracked by the version label, so a changed law under an unchanged version causes a "both say vX" divergence (the gap PR #24 hit, repaired by #25). This is the first law-PR to fully exercise §14.5 and it obeys §14.5 itself (the v3.51→v3.52 bump + this entry — the recursion is intentional). Source: spec-first (FLOWFORGE_ENGINE_SPEC §14.5). **Engine edit → the freeze-counter does NOT start.**
- 2026-06-21 — **Engine v3.50→v3.51 — clean-run & freeze-counter definition into the law (§14, spec-first п.3):** added the `### Clean-run & freeze-counter (engine-stabilization criterion)` subsection to CLAUDE.md section 11 — the engine-vs-task-output boundary, the cycle-run unit (one ticket, `/spec` → human merge inclusive), the two-level clean-run definition (L1 machine to PR-open: no engine edit / no manual code / no unplanned STOP / no hook bypass; L2 no human cleanup at merge: no objective `/review`-checklist defect returned for rework, stylistic nitpicks excepted), "3 clean = 3 different tickets", and the freeze-counter (starts at first clean run / resets on any engine edit / freezes at the target streak). The running count and whether it has started live HERE in this log, not in the law (law-vs-data). Closes the T18 gap — the definition previously lived only in journal entries (grep of CLAUDE.md §11 = 0 matches for freeze/clean/counter). The §14 text landed via PR [#24](https://github.com/oleksii-kyrsenko/flowforge-pilot/pull/24) (merged, commit `decdb9f`); this v3.51 follow-up adds the version bump + this log entry that PR #24 omitted (hard rule 7: an unrecorded change is unfinished). Source: spec-first (FLOWFORGE_ENGINE_SPEC §14). **Engine edit → the freeze-counter does NOT start (still NOT started; the first clean run starts it).**
- 2026-06-16 — **FF-9 (Icon primitive) cycle summary:** `/spec` → `/build` → `/review` → `/ship` complete; PR [#23](https://github.com/oleksii-kyrsenko/flowforge-pilot/pull/23) → `dev` (squash), awaiting human merge; Jira FF-9 → Review (PR-link comment + revised-spec comment posted). **Contract revised mid-build:** the first-approved uniform-24×24 spec was superseded by a **natural-viewBox contract** (each glyph keeps its verbatim Figma viewBox + stroke-width; `size` = rendered height with aspect preserved; `edit` ships non-square 16×15, `plus` inset) — re-approved before any component code; new Jira comment (id 10068) supersedes the original spec comment (id 10034, left intact). Shipped `Icon` (8 glyphs, all extracted verbatim via `download_assets format:svg`), `--size-icon: 24px` token, 19 unit tests; reviewer verdict SHIP. **Metrics note:** `metrics.csv` carries TWO `FF-9,build,start` rows — `2026-06-15T13:15:17Z` is **SUPERSEDED** (the first `/build` was aborted before any file was written, to land the icon-vector engine fix v3.48) by `2026-06-15T18:42:42Z`; the 13:15:17 row is retained (no backfill/delete), and cycle-time aggregation should use the **later** start (18:42:42) as the real build start. **Freeze-counter status:** NOT a clean cycle — the FF-9 arc drove engine edits (v3.48 icon-vector/§9 was triggered during FF-9's own `/build`; v3.49 + v3.50 followed in the same arc), so per the v3.41 definition and the FF-8 precedent (ruled not-clean for touching `eslint.config.mjs` mid-build) the FF-9 cycle touched `.claude/` + engine config. The 3-clean-cycle freeze counter still has **NOT** started; it (re)starts at the first **future** ticket cycle touching no `.claude/`/engine config — and the pending post-FF-9 engine PRs (hook allow-list, §9 follow-ups, git-commit-step codification) are themselves engine edits, so the counter cannot start through them either.
- 2026-06-15 (v3.50) — **Require an explicit Figma source URL for /baseline & /tickets; §8 Figma default removed (engine edit).** Two coupled changes. **(1) DATA:** deleted the CLAUDE.md §8 TODO "[x] Figma:" bullet — the project-specific two-URL-roles model (token source / ticket source) with hardcoded example URLs (fileKey `nwCQ1hdons94beObATiRv7`). No command consumed a default project Figma URL, so the entry configured nothing; as a hardcoded project value in CLAUDE.md it is exactly what the template must not carry. Supersedes the §8 two-URL-roles tracking. **(2) LAW:** `/baseline` and `/tickets` now require a source URL — added a canonical clause to CLAUDE.md §11 (both definitions) AND the mirrors `.claude/commands/baseline.md` + `tickets.md`: invoking with no Figma URL argument → STOP with a clear error ("provide a Figma file/page/frame URL"), never fall back to a default/project Figma URL. The error condition is strictly a ZERO-argument invocation; the per-source scope cascade is untouched — option (a) "tracker tickets' frame links" still expands a GIVEN source into frames. **Scan results:** the §8 bullet was the ONLY place asserting the two-URL-roles/default model — no other CLAUDE.md text depended on it (§5 line 103 is a generic Figma-MCP capability line; `/spec`'s no-arg fallback is ticket-local; progress-log mentions are historical), so nothing else needed reconciling; and neither command nor §11 had any pre-existing default/§8-fallback wording, so change (2) makes the requirement EXPLICIT rather than stripping a mechanism. After this edit the engine holds no project Figma URL anywhere; §11 + command files stay project-agnostic (no FF keys/URLs/hex). Header 3.49 → 3.50 (date unchanged). **Freeze-counter status:** engine edit (CLAUDE.md + `.claude/commands/` ship with the template), so the 3-clean-cycle freeze counter still has **NOT** started — same standing as v3.40–v3.49; it (re)starts at the first cycle touching no `.claude/` or engine config. PR: `chore/require-figma-url` → `dev` (squash); CLAUDE.md + `.claude/commands/baseline.md` + `.claude/commands/tickets.md` + progress-log.
- 2026-06-15 (v3.49) — **No auto-attribution convention (engine edit).** Codified that the agent never adds `Co-Authored-By:` trailers, nor `Generated with` / `🤖 Generated with [Claude Code]` lines, to commit messages OR PR bodies — both carry only substantive content. The harness default emits these; for this repo (which doubles as the `flowforge-template` source) they are history noise, and they had been removed by hand on the last two PRs (#19, #20) — this makes the standing convention explicit so no per-PR correction is needed. **Single-home placement:** the rule is CROSS-CUTTING (every commit/PR), so it lives ONCE in CLAUDE.md §11 "Code standards" (appended to the Commits bullet, which already defines the message format and thus governs its content) and is deliberately NOT duplicated into any `.claude/commands/*` file. **Nothing removed:** a repo-wide scan (`.claude/` commands/agents/skills/hooks/settings + CLAUDE.md + docs) found NO file that instructed adding an attribution line, so this is a pure addition. **Option B parked:** a `commit-msg`/`prepare-commit-msg` git hook to mechanically strip trailers was considered and deferred — instruction-layer rule only for now. CLAUDE.md header 3.48 → 3.49 (date unchanged). **Self-compliance:** this change's own commit and PR carry no `Co-Authored-By`/Generated-with line. **Freeze-counter status:** engine edit (CLAUDE.md ships with the template), so the 3-clean-cycle freeze counter still has **NOT** started — same standing as v3.40–v3.48; it (re)starts at the first cycle touching no `.claude/` or engine config. PR: `chore/no-auto-attribution` → `dev` (squash); CLAUDE.md + progress-log only.
- 2026-06-15 (v3.48) — **Icon/vector extraction: correct retrieval tool + deterministic anti-fabrication STOP (engine edit).** FF-9's `/build` tried to obtain icon geometry via the WRONG Figma MCP tool (`get_design_context`, which returns a raster `<img>` reference for icons), concluded "Figma exposes these icons only as flattened raster," and **hand-authored icon `<path d="…">` geometry** instead of stopping. Empirical investigation disproved the premise: vector IS retrievable via `download_assets` with `format:"svg"` on a PLACED-INSTANCE node — verified real path data on two icons (chevron-left `1:774` → `d="M15 18L9 12L15 6"`, stroke-width 2, round caps; plus `62:1268` → scaled instance, stroke-width 1.46813, consistent with Q21). The master `1:86` is not page-addressable. Narrow fix (icons/vector only — a general build-wide anti-fabrication system is explicitly PARKED): **(1)** `.claude/skills/design-extraction/SKILL.md` — new **§9 "Icon & vector-glyph extraction"** (retrieve vector via `download_assets` format:svg on the placed instance; `get_design_context`/screenshots are NOT vector sources and a raster result is NOT evidence the vector is unavailable — "wrong tool", try `download_assets` svg; isolate the inner `<g id="<name>"><path id="Icon">` glyph group and drop wrapper/background rects; scaled instances export scaled geometry → prefer a true full-size instance/master, else surface a design question, never silently normalize) + a pointer bullet in §1. This is the single home for the tool name + isolation technique, and corrects the stale "flattened raster" premise at the methodology layer. **(2)** CLAUDE.md §11 `/build` AND **(3)** `.claude/commands/build.md` `## Do` — a **word-for-word-identical** STOP rule added (verified byte-identical, modulo the bullet's bold lead-in): "Icons / vector assets: obtain glyph geometry via the design-extraction skill's vector-asset method; if vector geometry cannot be retrieved by any available tool → STOP and ask, never hand-author or guess path geometry (hard rule 5, at the build layer)." Both project-agnostic — no FF keys/node-ids/hex/tool name in §11 or build.md (those live only in the skill). **(4)** CLAUDE.md header 3.47 → 3.48 (date → 2026-06-15). **Out of scope (NOT touched this PR):** the same stale premise in `docs/specs/FF-9.md` and design-questions **Q24** — these are FF-9 build artifacts, corrected when the FF-9 build resumes with the correct extraction method, not in this engine PR. **Self-test (PASS):** re-ran `download_assets` svg on `1:774` → real `d="M15 18L9 12L15 6"`; `get_design_context` on `1:774` → raster `<img src=…>` (no `d=`), correctly classified "wrong tool → try download_assets / would STOP if truly unavailable," NOT a fabrication license. `npm run lint`/`typecheck`/`test:run` green (docs/engine-only; run as a guard). **Freeze-counter status:** engine edit (`.claude/` skill + command + CLAUDE.md ship with the template), so the 3-clean-cycle freeze counter still has **NOT** started — same standing as v3.40–v3.47; it (re)starts at the first cycle touching no `.claude/` or engine config. PR: `chore/icon-vector-extraction` → `dev` (squash); CLAUDE.md + `.claude/skills/design-extraction/SKILL.md` + `.claude/commands/build.md` + progress-log.
- 2026-06-14 (v3.47) — **PreToolUse hook now auto-allows the read-only `atlassianUserInfo` identity lookup (engine edit).** v3.46's `jira-transition-guard.sh` auto-allowed `/spec`'s forward transitions (21/31) and the assignee-only edit, but the `atlassianUserInfo` call that `/spec` makes to resolve the current user for that assignee claim still raised a permission prompt — an incomplete implementation of an already-accepted rule (hard rule 3 marks the whole assignee-claim advance "AUTO, no confirm"), not a new feature. Closed the gap with one ADDED allow case, no broadening of the transition/edit logic: **(1)** `.claude/settings.json` — added `mcp__atlassian__atlassianUserInfo` to the PreToolUse matcher alongside `transitionJiraIssue|editJiraIssue`. **(2)** `.claude/hooks/jira-transition-guard.sh` — new first branch `tool_name === "mcp__atlassian__atlassianUserInfo"` → decision **allow** (read-only, no args to inspect, zero write risk); header comment updated (two→three AUTO cases) and the SAFE-FAILURE invariant reworded (only the **three** exact cases ever emit `allow`); all existing branches and the unrecognized→`ask`/parse-failure→`ask`/`exit 0` safe-failure default are unchanged. **(3)** CLAUDE.md §11 "Ticket lifecycle & board statuses" Portability note extended to mention the hook also auto-allows the read-only `atlassianUserInfo` identity lookup — still instance-agnostic (the tool name is not FF-specific, unlike the ids). **Verify:** re-ran the v3.46 hook unit-check matrix (transition 21→allow, 31→allow, 51→ask, editJiraIssue assignee-only→allow, other-field→ask, malformed→ask) — all six unchanged — plus the new case (atlassianUserInfo payload→allow); `settings.json` valid strict JSON; `npm run lint`/`typecheck`/`test:run` green. **Freeze-counter status:** engine edit (`.claude/` hook + settings + CLAUDE.md ship with the template), so the 3-clean-cycle freeze counter still has **NOT** started — same standing as v3.40–v3.46; it (re)starts at the first cycle touching no `.claude/` or engine config. PR: `chore/hook-allow-userinfo` → `dev` (squash); `.claude/settings.json` + `.claude/hooks/jira-transition-guard.sh` + CLAUDE.md + progress-log.
- 2026-06-14 (v3.46) — **Jira permission gate (PreToolUse hook) + `/review` font-default-compliance law (one PR, engine).** **(1) Part A — PreToolUse hook closes the v3.45 deferred gap.** v3.45 recorded that auto-allowing `/spec`'s forward transitions (21 To Do, 31 In Progress) + the assignee claim while keeping `/ship`'s Review transition (51) under confirm is impossible via `settings.json` permission rules (MCP rules match tool NAME only — no argument-level matching — and the file is strict JSON, no comment marker), and that the only faithful mechanism is a PreToolUse hook inspecting `tool_input`. Added that hook: `.claude/hooks/jira-transition-guard.sh` (bash wrapper + `node -e` stdin JSON parser, house style; `chmod +x`), registered in `.claude/settings.json` as a `PreToolUse` block with matcher `mcp__atlassian__transitionJiraIssue|mcp__atlassian__editJiraIssue`. **Decision table** (verified against the current PreToolUse contract at code.claude.com/docs/en/hooks — output is `hookSpecificOutput.permissionDecision` ∈ allow/deny/ask/defer, `hookEventName:"PreToolUse"`; matcher with only letters/digits/`_`/`|` is exact-string alternation, not regex): `transitionJiraIssue` id 21/31 → **allow**, any other id (51/41/unknown) → **ask**; `editJiraIssue` with `fields` exactly `{assignee}` → **allow**, any broader/empty fields → **ask**; unknown tool / parse failure → **ask**. **SAFE-FAILURE invariant:** only the two exact cases emit `allow`; every other path (parse failure, missing fields, unknown tool, node failure) emits `ask`; the script always `exit 0` and the bash fallback emits an explicit `ask` if node yields nothing — a bug can only fail toward confirm, never toward auto-allow. Unit-checked with 6 payloads → allow/allow/ask/allow/ask/ask (shown at the checkpoint). **Portability:** the hook's hardcoded ids (21/31/51) + `mcp__atlassian__*` names are FF-specific; the §11 "Ticket lifecycle & board statuses" Portability note was extended to name the hook and mark itself the canonical id-generalization list (adopter rediscovers via `getTransitionsForJiraIssue`). A `PROJECT-SPECIFIC` comment is in the script. **(2) Part B — `/review` font-default compliance (engine law, agnostic).** Added verbatim to the §11 `/review` contract AND `.claude/commands/review.md` (`## Do` checklist): a component must not pin its own font-family (`font-<family>` class) unless the spec explicitly requires a non-default family; inheriting the global document default is the norm; `/review` flags any font-family class on a component and verifies it against the spec — an unjustified one is the stray-class bug. Generalizes the stray-`font-poppins`-on-`Button` fix (v3.45 item 5) into a permanent review check; no ticket keys, no font names. **(3)** CLAUDE.md version 3.45 → 3.46 (date unchanged). **Freeze-counter status:** engine edit (`.claude/` hook + settings + commands + CLAUDE.md ship with the template), so the 3-clean-cycle freeze counter still has **NOT** started — same standing as v3.40–v3.45; it (re)starts at the first cycle touching no `.claude/` or engine config. PR: `chore/jira-perm-hook-and-review-font-guard` → `dev` (squash); 1 new hook + `.claude/settings.json` + `.claude/commands/review.md` + CLAUDE.md + progress-log.
- 2026-06-14 — **§8 Task-tracker column list aligned with the §11 lifecycle table (docs-only, no version bump).** The §8 Task-tracker line listed `columns To Do → In Progress → Review → Done` — missing the **Backlog** column, which is the project's initial status (10010, per the §11 "Ticket lifecycle & board statuses" table and the v3.44 entry). Corrected to `columns Backlog → To Do → In Progress → Review → Done` so the two descriptions of the FF board match. TODO-line correction only — no engine/behavior change, so **no version bump** and no freeze-counter impact. PR: `chore/align-section8-columns` → `dev` (squash); CLAUDE.md + progress-log only.
- 2026-06-14 (v3.45) — **`/spec` UI-content guard + primary-font lifecycle codified; pilot Poppins wiring fixed (engine + pilot-repair, one PR).** **(1) `/spec` UI-content preflight** added verbatim to three engine files — CLAUDE.md §11 `/spec` definition (between the lifecycle advance and the Figma read), `.claude/commands/spec.md` (new `## UI-content preflight` section before `## Do`), and `.claude/agents/analyst.md`: if the ticket describes a visual/UI component AND no Figma frame link is available → STOP (don't fabricate design values / write a spec); honest non-UI tickets proceed with UI sections N/A and no Figma call. Closes the gap where `/spec` could invent tokens for a mockup-less UI ticket. **(2) Primary-font determination** added to `.claude/skills/design-extraction/SKILL.md` (new §8): the census tallies font families across all swept frames; the most frequent is the PRIMARY-FONT CANDIDATE (a hypothesis confirmed at the baseline checkpoint, like a near-match token); `/spec` NEVER determines the primary font (single frame too narrow) — it consumes the confirmed one; different-family frames → design question. **(3) Primary-font application guard** added to CLAUDE.md §11 (Token map maintenance): the confirmed primary font is applied ONCE, globally, as the document default — loaded via next/font in the root layout and set on `<body>`, never per-component; idempotent (no-op if already wired); fires only when a UI source exists. **(4) Jira permission gating — SKIPPED (human decision).** Goal was to auto-allow `/spec`'s transitions 21/31 + assignee claim while keeping `/ship`'s Review transition (51) under confirm. Verified against current Claude Code docs + tool schemas: `.claude/settings.json` is **strict JSON (no comments of any kind — the requested `<!-- -->` marker can't live there)**, and **MCP-tool permission rules match the tool name only — no argument-level matching**, so a plain `allow` for `mcp__atlassian__transitionJiraIssue` would also auto-approve 51 (the exact blanket effect the "don't-ask-again" path was rejected for). The only faithful mechanism is a **PreToolUse hook** inspecting `tool_input.transition.id` (`permissionDecision:"allow"` for 21/31, `"defer"` for 51/everything else). Per human decision this hook is **deferred as a known gap** — no `settings.json`/hook change this PR; the `/spec` 21/31/assignee calls keep raising a normal permission prompt (instruction layer still says "AUTO"). **(5) Pilot font wiring repaired** (project code, NOT engine): `src/app/layout.tsx` — dropped the create-next-app Geist fonts + "Create Next App" metadata; load **Poppins** via `next/font/google` (weights 300/400/500/600/700/900, `display:"swap"`, `variable:"--font-poppins"` so the self-hosted face populates the existing design-token custom property on `<html>`); FlowForge metadata. `src/app/globals.css` — body default `font-family: var(--font-poppins)` (was scaffold Arial); `@theme inline` Geist refs replaced (`--font-sans` → `var(--font-poppins)`, Geist mono line removed). The FlowForge `@theme` token block (incl. `--font-poppins:"Poppins",…`) is unchanged — no map↔`@theme` divergence. `src/components/ui/Button.tsx` — removed the per-component `font-poppins` class (inherits the global default per guard #3); `Button.test.tsx` — the `asChild` class-merge assertion switched from `font-poppins` to `inline-flex` (a still-present base class). **(6)** `docs/design-tokens.md` — the `--font-poppins` row annotated as **primary font (document default)**, loaded via next/font in the root layout (recorded in the data, not CLAUDE.md). **Freeze-counter status:** engine edit (CLAUDE.md + `.claude/` ship with the template), so the 3-clean-cycle freeze counter still has NOT started — same standing as v3.40–v3.44; it (re)starts at the first cycle touching no `.claude/` or engine config. PR: `chore/spec-guard-font-perms` → `dev` (squash); CLAUDE.md + 3 `.claude/` files + 4 src files + 2 docs.
- 2026-06-13 (v3.44) — **Jira ticket-status transitions wired into the pipeline (engine edit).** The board now reflects real work state for BOTH agent-run and manually-run tickets. Discovered the FF workflow transitions (read-only, no writes): global transitions reachable from any status — → To Do (Selected for Development) = **21**, → In Progress = **31**, → Review = **51**, → Done = **41**; column→status map Backlog(10010) → To Do col=Selected for Development(10008) → In Progress(3) → Review(10011) → Done(10009); new issues default to **Backlog** (project's initial status) with no transition. **CLAUDE.md §11 edits:** (a) new **"Ticket lifecycle & board statuses"** subsection (column-meaning table + transition-id reference + portability note: ids are FF-specific, a template adopter rediscovers them via `getTransitionsForJiraIssue`, assignee always resolved at runtime via `atlassianUserInfo`, never a hardcoded accountId; forward-only/idempotent; **Review → Done is a MANUAL human step** since merge is human-only — pipeline never moves to Done, interim until later board automation). (b) **`/spec`** now advances the lifecycle at the start (AUTO, no confirm): read status → if Backlog transition 21 → claim assignee via `atlassianUserInfo` if empty/not current user → transition 31 (In Progress); each step idempotent/forward-only. (c) **`/build`** gained a status preflight: In Progress → proceed; otherwise STOP, flag the anomaly (likely skipped `/spec` or wrong key), and confirm before transition 31. (d) **`/ship`** gained a precondition preflight before doc-writer/PR (In Progress + branch with commits ahead of dev + spec file present); missing any → STOP and flag — an ADDITIONAL gate that does NOT remove the existing PR/transition confirms. (e) **`/tickets`** create-status wording fixed "in To Do" → "default to Backlog; do NOT issue an explicit transition" (matches actual behavior — FF-9…FF-22 landed in Backlog). (f) **Hard rule 3** records the deliberate asymmetry: `/spec` forward advances are automatic (launching `/spec` = intent to start); result-exposing/anomalous changes still confirm (`/ship` →Review, `/build` & `/ship` preflights). **Command files** `.claude/commands/{spec,build,ship,tickets}.md` wired to match (lifecycle/preflight sections + tickets wording). **Freeze-counter status:** engine edit (CLAUDE.md + `.claude/commands/` ship with the template), so the 3-clean-cycle freeze counter still has NOT started — same standing as the prior engine edits (v3.40–v3.43); it (re)starts at the first cycle touching no `.claude/` or engine config. PR: `chore/ticket-lifecycle-transitions` → `dev` (squash); CLAUDE.md + 4 command files + progress-log only.
- 2026-06-13 (v3.43) — **`/design-fixes export` branch hardened (engine edit).** Two fixes to `.claude/commands/design-fixes.md`: (a) **output dedup invariant** in the export branch — each open question must render EXACTLY ONCE; before returning, verify rendered-question count == number of OPEN questions in `docs/design-questions.md` (count ⏳-open QUESTION entries only, excluding the status-legend line) and that no question number repeats, fixing before output if not (a real export run had duplicated the Typography→Motion→Button-states block). (b) **metrics carve-out** — clarified that `/design-fixes export` is read-only and does NOT write `docs/metrics/metrics.csv` (a `design-batch-export` pseudo-ticket would pollute per-ticket cycle aggregation, like the `/tickets`/`/baseline` rows removed in v3.40); the Metrics header is now "designer-reply mode only" and only a reply-applying run (branch + PR) writes metrics. **CLAUDE.md §"Metrics & logging"** writer-list sentence got the same export carve-out so the next run doesn't attempt the row again. **Freeze-counter status:** engine edit (CLAUDE.md + `.claude/commands/` ship with the template), so the 3-clean-cycle freeze counter still has NOT started — same standing as the prior engine edits (v3.40–v3.42); it (re)starts at the first cycle touching no `.claude/` or engine config. PR: `chore/design-fixes-export-hardening` → `dev` (squash); CLAUDE.md + `.claude/commands/design-fixes.md` + progress-log only.
- 2026-06-13 (v3.42) — **Human-only merge codified as a deliberate design decision (Hard rule 1).** Added one sentence under Hard rule 1: human-only merge is a deliberate design decision (a demo feature — "AI never merges on its own"), NOT a temporary limitation; do not propose automating or delegating merges. Engine doc (CLAUDE.md ships with the template). **Cycle-1 engine-doc clarification** — does NOT change the freeze-counter status: the 3-clean-cycle freeze counter still starts only at the first cycle that touches no `.claude/` or engine config (per v3.41). PR: `chore/merge-decision-doc` → `dev` (squash); CLAUDE.md + progress-log only. (Sequencing note: branched from `dev` at v3.40 while FF-8 was in flight; FF-8 (v3.41) merged first, so this branch was rebased onto `dev`@v3.41 and kept version 3.42 — no number skipped.)
- 2026-06-13 — **FF-8 (Button) cycle summary:** `/spec` → `/build` → `/review` → `/ship` complete; PR [#12](https://github.com/oleksii-kyrsenko/flowforge-pilot/pull/12) → `dev` (squash), awaiting human merge; Jira FF-8 → Review pending confirmation (hard rule 3). Not a clean freeze-counter cycle (touched `eslint.config.mjs`, per v3.41).
- 2026-06-13 (v3.41) — **FF-8 Button — first `/build` (cycle 1).** Implemented the Button primitive: `src/components/ui/Button.tsx` (variants `filled` gradient CTA / `outline` "Read More", `asChild` slot rendering, native `<button>` default + `type` guard), co-located `Button.test.tsx` (12 unit tests, one per acceptance criterion), and `src/components/ui/index.ts` barrel. **Token-only** styling — every fill/shadow/color/spacing rides existing `@theme` tokens (gradients via `bg-[image:var(--gradient-*)]`); **no new design tokens** (spec FF-8 §5 proposed none, so `@theme` ↔ `docs/design-tokens.md` unchanged). Gate green: `npm run lint`, `npm run typecheck`, `npm run test:run` (15 tests total pass). **New dependency:** `@radix-ui/react-slot ^1.2.5` (for `asChild` prop/ref merging) — current API verified via Context7 per the spec note, peer-supports React 19, and its dist carries **no `'use client'`** directive, so `Button` stays a shared/server-capable component. **Disabled-state note:** the approved spec proposed `--color-on-dark-40` for the disabled label, but the outline variant uses `background-clip:text`, so a per-variant text-color swap conflicts; implemented uniform `disabled:opacity-50` instead — within the "proposed default" latitude since Q17 (disabled appearance) is still open. **Engine-config change (Option A, human-approved):** `eslint.config.mjs` `react/require-default-props` → `["error", { functions: "defaultArguments" }]` — React 19 dropped `defaultProps` for function components, so defaults are enforced via default arguments instead of the dead `defaultProps` static. **eslint.config.mjs is engine config (ships with the template).** **Freeze-counter status:** this is a **cycle-1 engine-config adjustment** — the 3-clean-cycle freeze counter does NOT start until `.claude/` and engine config (incl. `eslint.config.mjs`) stop changing; it (re)starts at the first cycle that touches no engine/`.claude` config. All changes kept on branch `flowforge/FF-8-button`; nothing on `dev`. Next: `/review`, then `/ship`.
- 2026-06-13 (v3.40) — **metrics.csv writers scoped to the five cycle commands.** `docs/metrics/metrics.csv` exists for per-ticket cycle-stage timing (the before/after demo artifact); `/tickets` and `/baseline` are setup/batch steps with no cycle, so their pseudo-rows polluted aggregation. Scoped the writer list to the five cycle commands. **CLAUDE.md §"Metrics & logging (automated)"**: writer list changed from `(/spec, /build, /review, /ship, /design-fixes, /baseline, /tickets)` → `(/spec, /build, /review, /ship, /design-fixes)`, plus a clarifying sentence — setup/batch commands (`/tickets`, `/baseline`) do NOT write metrics.csv; they record activity in `docs/progress-log.md`. **`.claude/commands/tickets.md`** and **`.claude/commands/baseline.md`**: deleted the entire "Metrics (mandatory — first and last action)" block from each (tickets already logs to the progress log; baseline records to the progress log if needed). The five cycle-command files, GUIDE Prompt 7, and section 7 wording were left untouched (they already consume metrics.csv as cycle-only). Header version field synced 3.0 → 3.40 to match the running log counter (it had drifted). **Resolves the pending reconciliation flagged in v3.39.** Freeze-counter marker: pre-cycle law reconciliation; touches `.claude/` setup commands only, NOT cycle-engine; 3-clean-cycle freeze counter starts at first `/spec` (FF-8). PR: `chore/scope-metrics-writers` → `dev` (squash). (Bundled in the same PR: the v3.39 backlog log entry, which had no PR of its own since `/tickets` opens none.)
- 2026-06-13 (v3.39) — **Homepage backlog created: FF-8..FF-22 (15 Stories, 42 SP).** Generated PM-style from the Website Figma file (`FlWYwSHj7vtd487hdXZi6t`, "Homepage" page) via `/tickets`. Scope confirmed with the human = **Homepage + shared primitives** (the file's other 6 routes — Our CARS, Booking, Payment, My Bookings, Profile, Terms, on the "All" page — are out of scope for this batch; the primitives seed them). One ticket = one component, with a shared-primitives scan so no two tickets build the same primitive. **9 primitives** (`src/components/ui/`): FF-8 Button, FF-9 Icon, FF-10 SectionHeading, FF-11 CarCard, FF-12 FeatureCard, FF-13 Accordion, FF-14 BrandLogoStrip, FF-15 Header, FF-16 Footer. **5 homepage sections** that reuse them: FF-17 Hero Banner, FF-18 Our Top Cars (carousel logic kept in-section this batch; extract to `ui/Carousel` later if a route reuses it), FF-19 Elite Fleet intro, FF-20 Why Us, FF-21 FAQ. **1 route assembly:** FF-22 Homepage (route-level → SEO requirements line). All created in project FF status **To Do** via Atlassian MCP in ADF (taskList acceptance criteria + clickable Figma frame links + cross-ticket reuse notes by key). **Metrics note (deviation from CLAUDE.md §"Metrics & logging"):** per the human, `/tickets` did NOT write `start`/`done` rows to `docs/metrics/metrics.csv` — only the five cycle commands (`/spec` `/build` `/review` `/ship` `/design-fixes`) write metrics; this batch is logged here only. CLAUDE.md §"Metrics & logging" and `.claude/commands/tickets.md` still list `/tickets` (and `/baseline`) as metrics writers and need reconciling to match this decision (pending human confirmation).
- 2026-06-13 (v3.38) — **`/design-fixes` VALUE-change wording aligned with the Tailwind-v4 theme source.** Fixed a residual v3.33 leftover in the `/design-fixes` definition (§11 "Roles and commands"): the VALUE-change clause still said "edit the map + tailwind config" / "a config-only change would break the build" — phrasing that predated the v3.34 Tailwind-v4 decision. Reworded to "edit docs/design-tokens.md + the Tailwind theme source" and "a theme-only change would break the build" (COLLAPSE/RENAME now reads "edit map/theme"), matching the version-aware Token map maintenance law nearby. Wording change only; no behavior change.
- 2026-06-13 (v3.37) — **`/tickets` accepts any input granularity.** Clarified the `/tickets` definition (§11 "Roles and commands"): inputs accept file, page, or frame URLs, mixed (same as `/baseline`) — the command expands a file/page to its frames itself via the scope cascade (list pages, skip empty/service pages). The exact per-ticket Figma frame link is an OUTPUT requirement (so `/spec` later knows what to read), NOT an input constraint — the human is never asked to pre-collect frame links, and a whole-page URL is the normal fast path for bulk backlog creation. Wording change only; no behavior added beyond what the scope cascade already implied.
- 2026-06-12 (v3.36) — **Law-vs-data split.** The progress log and the filled design-token-map table were externalized out of CLAUDE.md into `docs/progress-log.md` and `docs/design-tokens.md` — CLAUDE.md now keeps only the law (commands, hard rules, conventions, maintenance rule) and stays constant-size, while the two growing journals live alongside `docs/design-questions.md` and `docs/metrics/`. Rationale: CLAUDE.md is loaded whole into context every session, whereas the journals are read per task — an unbounded log in CLAUDE.md would inflate every session's context. Section 9 became a stub pointing here; the section 11 "Design token map" subsection became a stub pointing to `docs/design-tokens.md` (the "Token map maintenance" law stayed in §11). All "section 9" / "this file as the log" references rewritten across CLAUDE.md and the command files (`ship.md`, `baseline.md`, `design-fixes.md`, `build.md`): progress-log entries now go to `docs/progress-log.md`, the never-diverge rule is now `docs/design-tokens.md` ↔ the `@theme` block, and the project-structure block lists both new journals. PR: `chore/externalize-log-tokens` → `dev` (squash).
- 2026-06-12 (v3.35) — **Prompts B and A promoted to pipeline commands `/baseline` and `/tickets`.** The two PM-style bootstrap prompts from the GUIDE (B = token baseline, A = backlog generation) are now first-class slash commands with their authoritative definitions moved into this file (§11 "Roles and commands", after `/design-fixes`); the GUIDE sections are reduced to stubs that point here. **`/baseline`** carries the full VERBATIM extraction contract: scope cascade (ticket links → explicit frame list → self-exploration), RE-RUN delta mode (new / "canon moved" / "deprecate?" against the existing map + `docs/design-questions.md`, with "No changes against the approved map" as the no-op path), and a mandatory CHECKPOINT (page inventory incl. a NOT VISIBLE TO TOOL bucket, full sectioned map with empty sections printed, pending list, draft questions) before any write; APPLY branches `chore/token-baseline`. **`/tickets`** generates a varied-complexity backlog with a SHARED-PRIMITIVES scan (a dedicated early "ui primitives" ticket or explicit "Reuses X from KEY" notes — never two tickets building the same primitive), shows drafts, STOPs for approval, then creates FF tickets in **To Do** via Atlassian MCP (ADF). Both commands now write `start`/`done` metrics rows (ticket column `baseline` / `tickets` unless a JIRA key applies) — §11 "Metrics & logging" command list extended; §5 "Slash commands" list extended → **7 commands total**. Command files `.claude/commands/baseline.md` and `.claude/commands/tickets.md` created on the existing pattern (frontmatter + defer-to-§11 body + metrics + restated hard rules: checkpoint STOP, human-only merge, ADF for Jira writes). **Note:** a post-merge cleanup command was considered and **rejected** — merge stays human-only per hard rule 1. (The earlier version-aware `tailwind.config` → "Tailwind theme source" alignment of the command/agent files already landed at v3.34; nothing to change there.) **Frontmatter convention (one source of truth):** a command file's `argument-hint` MUST be a verbatim copy of that command's argument signature in the §11 definition — no paraphrases; a command with no arguments omits the field entirely. Aligned all seven files to this: dropped `review.md`'s stray `argument-hint: [JIRA-KEY]` (§11 `/review` takes no arguments) and corrected `design-fixes.md` from the paraphrased `<designer reply text | export>` to `<designer reply | export>`; the other five already matched. Any command created later must follow this. PR: `feat/baseline-tickets-commands` → `dev` (squash).
- 2026-06-12 (v3.34) — **Design token baseline applied (GUIDE Prompt B, verbatim mode).** Sources: Components (`JFKSRWAuZTSR4nUMNKe0Gn`) + Website (`FlWYwSHj7vtd487hdXZi6t`). Filled the §11 Design token map in place and wrote the matching **`@theme` block in `src/app/globals.css`** — this project is **Tailwind v4**, so the theme source is CSS-first `@theme`, NOT `tailwind.config theme.extend` (no JS config scaffolded; `npm run build` green). Reworded the token-map heading parenthetical and the Token-map-maintenance `/build` bullet to be **version-aware** ("the Tailwind theme source [v4: `@theme` in the global stylesheet; v3: `theme.extend` in tailwind.config]"); the never-diverge pair is now the map ↔ the `@theme` block. Tokens: ~27 colors (incl. a 5-state booking **status-badge palette** `#54a8d7`/`#008f28`/`#bba145`/`#953c3c`/`#7c7c7c`), 7 gradients (documented `--gradient-*` custom properties), 4 shadows + `--blur-glass: 20px` (BACKGROUND_BLUR, 14 nodes) + `--blur-divider: 1px`, full Poppins type scale + 2nd/3rd families (**Proxima Nova** passwords, **SF Pro Text** Apple Pay), radii `5`/`8` (⚠ pending — 0 dominates elsewhere), `--duration-base: 300ms` (0.3s ×46; the 1.2s banner auto-advance is Banner-spec, not a global token), letter-spacing `1.82px`, breakpoints `360`/`1440`. **Coverage is a partial sweep, not full:** the Website file has **19 pages**; `get_design_context` surfaced a subset — unswept pages (Privacy, Sign in, Almost-behind-the-wheel, About us, Errors, etc.) are accepted residual risk picked up per-ticket by `/spec`. **Excluded as artifacts** (never tokenized): `#9747ff` (Figma annotation purple), "Inter Extra Bold 64" (`4004:79` spec label), and — per the new hidden-layer rule — `#ff3d2e` (lives only on hidden layers `62:2065`/`62:2073`) and `#ffc700` (rating-star yellow, ~84 uses in the 2026-06-11 revision, **zero today** — design changed between runs). Created `docs/design-questions.md` with **Q1–Q16** (⏳ open; Q14 ✅ resolved by the verification census — blur confirmed & tokenized). **Skill edit (same PR):** added a hidden-layer **visibility policy** to `.claude/skills/design-extraction/SKILL.md` §2 — the census counts rendered (visible) nodes; hidden-only values are reported on a separate HIDDEN line per category and never tokenized, becoming design questions (today's `#ff3d2e` is the precedent). **Command-file alignment (same PR):** the bootstrap-era command/agent files predated the v4 decision and hardcoded `tailwind.config`/`theme.extend` — reworded to the version-aware phrasing in `.claude/commands/build.md` (token-apply step), `.claude/commands/design-fixes.md` (VALUE-change line), and `.claude/agents/doc-writer.md` (forbidden-paths list). Also fixed `design-fixes.md`'s metrics row from the garbled `design,fixes,start,<TS>` to `<batch-label-or-JIRA-KEY>,design-fixes,start,<TS>` — the stage column must be the full command name `design-fixes`, not a stage named `fixes` (which would break the weekly CSV aggregation). PR: `chore/token-baseline` → `dev` (squash). From here the map grows ONLY via `/spec`.
- 2026-06-12 (v3.33) — **Pipeline skeleton bootstrapped.** Created the operating machinery the docs describe: 5 slash commands (`.claude/commands/{spec,build,review,ship,design-fixes}.md` — each defers to §11, takes `$ARGUMENTS`, and writes `start`/`done` rows to `docs/metrics/metrics.csv`); 3 subagents (`analyst` read-only+MCP, `reviewer` read-only, `doc-writer` writes only CHANGELOG/README/docs); 3 skills (`design-extraction`, `seo`, `adf-formatting` — the last wired into the Jira-writing flows `/spec` and `/ship`, not doc-writer); `.claude/settings.json` with a PostToolUse hook (`.claude/hooks/post-edit-checks.sh` runs lint+typecheck on `.ts/.tsx` edits) and `.env*` deny rules (enumerated so `.env.example` stays agent-editable). **Source layout migrated `app/` → `src/app`** to match the canonical structure (`@/*` → `./src/*`). Tooling: ESLint 9 flat config + Prettier (Airbnb via `eslint-config-airbnb-extended` — see Code style for the preset decision); husky pre-commit (`lint-staged && typecheck`) + pre-push (`test:run`); Vitest+RTL+jsdom and Playwright (browsers installed), e2e kept out of hooks; `.env.example` + gitignore `!.env.example` exception. Chosen current-stable versions: airbnb-extended 3.1.0, eslint-config-prettier 10.1.8, prettier 3.8.4, husky 9.1.7, lint-staged 17.0.7, vitest 4.1.8, @vitejs/plugin-react 6.0.2, jsdom 29.1.1, @testing-library/{react 16.3.2, dom 10.4.1, jest-dom 6.9.1, user-event 14.6.1}, @playwright/test 1.60.0 (Vitest resolves `@/*` via native `resolve.tsconfigPaths`). **`eslint-config-next` removed** as unused — airbnb-extended's `next` config supplies the `@next/next` Core Web Vitals rules (21 rules active, verified via `eslint --print-config`); keeping both would double-register plugins. **Env deny rules enumerate the full Next.js env set** (`.env`, `.env.local`, `.env.*.local`, `.env.{development,production,test}`) for Read/Edit/Write rather than a single `.env*` rule, because Claude Code evaluates deny-over-allow with no negation — a broad rule would block the agent-maintained `.env.example`; layered with `.gitignore` (`.env*` + `!.env.example`) and the §11 instruction.
- 2026-06-12 (v3.32) — **Skills layer adopted** — three skills (design-extraction / seo / adf-formatting) created at bootstrap; origin rule (new subagent only for a new rights combination; shared knowledge → skill; law never moves into skills); no dedicated SEO subagent (v3.18 upheld); prompt order changed to bootstrap → token baseline → tickets so Prompt B reads the skill; Prompt B gained a RE-RUN delta mode (GUIDE).
- 2026-06-11 (v3.31) — **Verbatim design-source model adopted** (mockup is canon; near-matches and anomalies become designer questions in docs/design-questions.md instead of pipeline decisions; reincarnated values get follow-ups); 5th command `/design-fixes` added (export + answer routing: config-only vs class replacement vs ticket); `/spec` extended (variant-set state lookup, reuse check, Animations line); `/build` limited to CSS-only animations; `/review` watches pending tokens and duplicated primitives; English-artifacts rule; structure + docs/design-questions.md. Designed and validated in chat session 2 via a dry-run census on the real Components + Website mockups.
- 2026-06-11 (v3.30) — **Merge-method policy adopted** (hard rule 1): PRs into `dev` are squash-and-merged (one ticket = one commit; PR title = commit message); release PRs `dev` → `main` use merge commits only (never squash) to keep histories compatible; `main` is the release branch — release PRs opened and merged by the human only, the pipeline never targets `main`. `/ship` PR titles switched to conventional-commit format (`feat|fix|chore(<JIRA-KEY>): <description>`) since the squashed title becomes the dev commit message. Enforcement: GitHub branch rulesets (dev: squash only; main: merge only) + auto-delete head branches + squash message default "PR title", configured by the human.
- 2026-06-11 (v3.29) — Token map heading aligned with the updated GUIDE Prompt B: parenthetical changed from "appends the table here" to "fills this table after an approval checkpoint and applies theme.extend to tailwind.config via a PR" (Prompt B now fills the EXISTING empty table via a chore/token-baseline branch → PR to dev, removing the duplicate-subsection risk the old wording invited). Cosmetic; no behavior change.
- 2026-06-11 (v3.28) — **Incremental token map maintenance adopted** (s.11, new "Token map maintenance (incremental)" subsection after the Design token map table): the map grows via `/spec` only — the analyst proposes role-based semantic tokens (`brand-*`/`surface-*`/`text-*`/`spacing-*`/`radius-*`) with Figma origin and raw value in a "Proposed token additions" spec block, after a mandatory value-normalized dedup check (exact match → reuse; near-match → flag for human canonicalization). On approval, `/build` applies additions to `tailwind.config` `theme.extend` and appends the table rows in the same PR (must never diverge; `/review` verifies map↔config consistency); raw values are forbidden in component code (`/review` flags arbitrary values); each addition is logged in s.9 as part of the ticket's cycle entry.
- 2026-06-10 (v3.27) — **Jira content format convention adopted** (hard rule 8): all Jira writes (descriptions, comments) must use `contentFormat: "adf"` (Atlassian Document Format JSON), never raw Markdown. Fixes checkboxes and links in FF-2..FF-7 descriptions; prevents `/spec` comments from repeating the issue.
- 2026-06-10 (v3.26) — **Backlog created:** FF-1..FF-7 (18 SP, full landing page) generated PM-style from Figma frames and created in Jira via Atlassian MCP; FF-5 split from FF-4 at review; ticket source file = Landing page (fileKey `nwCQ1hdons94beObATiRv7`). Figma URLs filled in §8.
- 2026-06-10 (v3.25) — Constraint recorded: GitHub Free → branch protection only on **public** repos; pilot repo `flowforge-pilot` is public by decision (protection > privacy for a pilot; secrets are excluded by the .env policy). GUIDE §3 got the plan note; §8 repo item filled.
- 2026-06-10 (v3.24) — **MCP config moved to project scope.** The three servers (atlassian, figma, context7) are added with `--scope project`, producing a committed `.mcp.json` in the repo root — the MCP config is now part of the engine and ships with the template (added to phase-3 composition and extraction prompt; template SETUP simplified: approve `.mcp.json` + `/mcp` auth instead of three `claude mcp add` commands). OAuth tokens are NOT in the file — they stay local per user. GUIDE §4 commands updated with the flag and a trust-prompt note. Discovered during real day-1 setup (the default `local` scope kept the config out of the repo).
- 2026-06-10 (v3.23) — Sync audit #5 (post-merge, post-metrics). Fixed: (1) template canonical structure was missing `docs/metrics/` and `tests/e2e/` — added; (2) the algorithm's standalone weekly hygiene prompt duplicated GUIDE Prompt 7 (hygiene merged there at v3.21) — replaced with a reference; (3) chat context refreshed: inventory version, decision renumbering (metrics = #14), stale status text (v2.4). Also this session: §8 Figma entry kept as an OPEN item with two URL roles (token source / ticket source), file names intentionally not locked — final choice on day 1.
- 2026-06-09 (v3.22) — **Metrics automated.** "Logging" replaced by "Metrics & logging (automated)": every pipeline command appends `start`/`done` timestamp rows to `docs/metrics/metrics.csv` (ticket,stage,event,timestamp_iso via `date -u`) as its first/last action; `/ship` adds a one-line cycle summary to s.9; weekly Prompt 7 aggregates the CSV into s.7 (preferring `human_corrected` rows). Human intervals (spec→build, PR→merge) are wall-clock approximations; GUIDE Prompt 6 repurposed from mandatory logging to optional human-effort correction. `docs/metrics/` added to canonical structure; GUIDE Prompt 2 wires the CSV at bootstrap; mirrored to template, playbook, chat context.
- 2026-06-09 (v3.21) — **Second documentation merge:** START_HERE + QUICKSTART → `FLOWFORGE_GUIDE.md`, the single technical guide (tools → repo creation → one-time setup → 48-hour route → all prompts: 1–7 plus A/ticket-generation and B/token-mapping → Claude Code tips → pitfalls → principles & environment → Jira Appendix A). Rationale: the v3.20 patch had blurred the boundary (prompts in two files, duplicated setup steps); duplication was the root cause of every desync caught in audits. 6 documents now; prompt numbering preserved (1–7, A, B); all live cross-references in playbook, this file, and chat context retargeted to GUIDE anchors (historical log entries left as written).
- 2026-06-09 (v3.20) — **Documentation restructure + setup-gap patch (5 items):** (1) BOOTSTRAP_PLAN merged into START_HERE v2 as Appendices A (own Jira), B (PM-style ticket prompt), C (token-mapping prompt) — 7 documents now, no duplicated day-one path; environment decision condensed to one line; template composition moved to TEMPLATE_ALGORITHM phase 3. (2) Quickstart step 0.5: create-next-app + dev branch + branch protection if the repo doesn't exist yet. (3) Prompt 2: `npx playwright install` for browsers + new step 8 — current STABLE versions for all added deps via Context7. (4) Token-map prompt now also APPLIES theme.extend to tailwind.config and verifies classes compile. (5) Template SETUP (extraction prompt): create `dev` + enable branch protection. All live cross-references retargeted (historical log entries left as written).
- 2026-06-09 (v3.19) — **Context7 MCP added** as the third connector: up-to-date library docs/versions for `/build` and for setup-time selection of current stable versions (template rule: install latest stable at installation, record the version set). Quickstart §1/Prompt 1, playbook, START_HERE, template SETUP references updated to three `claude mcp add` commands.
- 2026-06-09 (v3.18) — **Senior engineering standards adopted** (s.11): Claude Code acts as senior FE/QA/analyst/SEO; new "Performance, Next.js usage & SEO" subsection — server-first RSC with leaf-level `'use client'`, full use of installed Next.js version capabilities (checked via package.json), performance budget enforced by `/review` (bundle, boundaries, CWV patterns), SEO requirements line in `/spec` for route-level tickets (Metadata API, semantics, JSON-LD where warranted). No dedicated SEO subagent in MVP (analyst+reviewer cover it; `seo` module = phase-6 candidate). `/spec` and `/review` definitions extended.
- 2026-06-09 (v3.17) — Decision: **Playwright MCP is NOT needed for MVP** — e2e specs are code, run via terminal; Claude Code's standard tools cover generation, execution, and trace reading. Playwright MCP (live browser control) parked as a phase-6 optional module idea: visual design-vs-implementation verification (Figma MCP screenshot vs rendered component) and interactive e2e debugging.
- 2026-06-09 (v3.16) — Testing policy adopted (s.11 new subsection; mirrored to template & quickstart): stack fixed (Vitest+RTL for unit/integration, Playwright for e2e); `/build` generates tests by level; level selection happens in `/spec` as a "Test plan" line approved with the spec; traceability rule — every acceptance criterion maps to ≥1 test, `/review` verifies the mapping; e2e runs in CI/manually, never in git hooks. `tests/e2e/` added to canonical structure.
- 2026-06-09 (v3.15) — Env/secrets policy adopted (s.11 new subsection, mirrored to CLAUDE.template.md): real `.env*` = human-only, agent blocked technically via settings.json deny rules; agent maintains `.env.example` (placeholders+comments) and updates it in the same change whenever an env var is introduced/renamed/removed; no real secret values anywhere the agent writes; MCP tokens live in Claude Code, not `.env`. Quickstart Prompt 2 extended with step 6 (gitignore, .env.example, deny rules).
- 2026-06-09 (v3.14) — Deep check #3. Fixed: playbook pointed the token map to §5 (that's the TEMPLATE layout); in the pilot it lives in §11 — reference corrected, and an explicit empty "Design token map" stub subsection added to s.11 as the unambiguous landing spot for the BOOTSTRAP_PLAN §3 prompt. Verified clean: section headers 1–11 (pilot) and 1–6 (template) intact; every §/phase/prompt cross-reference in playbook and START_HERE resolves; hard rules consistent.
- 2026-06-09 (v3.13) — Double-check pass. Fixed: (1) playbook stage 2/3 dates re-aligned with the s.6 plan (Jun 16–29 / Jun 30–Jul 13; being ahead of schedule via START_HERE is encouraged); (2) README.template.md was referenced by CLAUDE.template.md but missing from the template composition — added to BOOTSTRAP_PLAN §6 and to the extraction prompt (algorithm phase 3, items 1 & 4). Verified clean: all 7 file cross-references resolve, command set is exactly /spec /build /review /ship, doc-writer present in all files, no stale agent counts or corporate-Jira remnants.
- 2026-06-09 (v3.12) — Second sync audit after doc-writer promotion: fixed stale agent counts in START_HERE (3 agents) and BOOTSTRAP_PLAN §6 (doc-writer added to template composition); plan row Jul 14–27 reworded (doc-writer exists from bootstrap; July = polishing its outputs). PLAYBOOK rewritten as v2: every action now has what/how/why detail; template extraction+validation moved explicitly into stage 4; demo Q&A prep added to stage 5.
- 2026-06-09 (v3.11) — Decision reversed (supersedes v3.10): **doc-writer promoted to MVP** as the third subagent. Scope: PR descriptions, changelog, README on structural changes; write access limited to documentation files (CHANGELOG.md, README.md, docs/), never source code. `/ship` delegates docs to it, then handles gh/Jira actions. Updated: pipeline diagram, s.5, `/ship` (s.11), CLAUDE.template.md, Quickstart Prompt 2.
- 2026-06-09 (v3.10) — Subagent roster aligned: two subagents in MVP (analyst: read-only+MCP; reviewer: read-only code review). Documentation duties folded into `/ship`; dedicated doc-writer subagent moved to optional extensions (template algorithm, phase 6). Pipeline diagram and s.5 updated.
- 2026-06-09 (v3.9) — Full documentation audit. Recorded the previously unlogged decision: own Jira Cloud Free (project FF) instead of corporate, tickets generated PM-style via prompt. Playbook stage 0 updated accordingly; stage 2 got a pre-commit smoke check. `FLOWFORGE_START_HERE.md` created: document map + 48-hour fast path; single entry point for the pilot.
- 2026-06-09 (v3.8) — Tooling adopted: ESLint+Prettier (Airbnb style; compatibility with ESLint 9 flat config to be verified at setup) and husky+lint-staged pre-commit (eslint --fix, prettier, tsc --noEmit; full tests on pre-push/CI). Layering defined: Claude Code hook = fast feedback, husky = enforcement gate, CI = final word; `--no-verify` forbidden for the agent. Mirrored into CLAUDE.template.md. Quickstart Prompt 2 extended with step 5: tooling setup during bootstrap.
- 2026-06-09 (v3.7) — Canonical project structure defined (s.11: components/ui + feature, co-located tests, hooks/lib/types, naming) — `/build` must follow it. README.md role defined: human onboarding doc, auto-updated by `/ship` only on STRUCTURAL changes (new directory/pattern/convention/command), not on routine additions. Same added to CLAUDE.template.md.
- 2026-06-09 (v3.6) — Reference `CLAUDE.template.md` drafted (`FLOWFORGE_CLAUDE_TEMPLATE.md`): 6 sections (concept, project config table, architecture, operating instructions, empty token map with generation prompt, empty log), all specifics as {{PLACEHOLDERS}}, challenge-related content excluded by design. To be used as the target shape in extraction (phase 3).
- 2026-06-09 (v3.5) — Sync procedure for the EXISTING template repo added (algorithm doc, phase 6): diff-based engine-vs-specifics classification with approval checkpoint, placeholder conversion for new values, mandatory CHANGELOG/UPGRADING update, audit grep, smoke test. Phase 3 remains one-time bootstrap only.
- 2026-06-09 (v3.4) — Template extension model defined (algorithm doc, phase 6): pilot-first flow, optional modules for new roles/stacks (stable small core), CHANGELOG+UPGRADING for distributed copies, Claude Code plugin as the future distribution path (post-challenge), scope freeze until Sep 04.
- 2026-06-09 (v3.3) — Template creation algorithm documented (`FLOWFORGE_TEMPLATE_ALGORITHM.md`): two-repo model (pilot = test bench, template = product), 3 hygiene rules during development, weekly hygiene-audit prompt, engine freeze criterion, extraction prompt + cleanup checklist, mandatory sterile-repo validation, publication & versioning flow.
- 2026-06-09 (v3.2) — Decision: a clean reusable template (`flowforge-template`) is an official deliverable. Separation rule adopted from day one: `.claude/` = project-agnostic, CLAUDE.md = project-specific. Extraction plan and prompt added to `FLOWFORGE_BOOTSTRAP_PLAN.md` (section 6).
- 2026-06-09 (v3.1) — Branching policy fixed: all branches are created from up-to-date `dev`; changes enter `dev` only via PR; direct pushes to `main`/`dev` forbidden. Updated: pipeline diagram (s.4), `/build`, `/ship`, hard rule 1 (s.11).
- 2026-06-09 (v3.0) — Context translated to English; mandatory maintenance rule added (every change must be recorded in this file). Quickstart file with ready-made Claude Code prompts created (`FLOWFORGE_QUICKSTART.md`).
- 2026-06-09 (v2.1) — Timeline clarified: 2-month development window (until ~Aug 09); Sep 04 — showcase, voting, winner announcement. Plan rebuilt, ambition levels adjusted.
- 2026-06-09 (v2.0) — Project named **FlowForge**; company corrected to EnGenious Inc; context cleaned of external references.
- 2026-06-09 (v1.1) — Stack confirmed: FE dev, Next.js/TS/Tailwind, Figma, Jira, GitHub. Concept refined: "Jira ticket + Figma mockup → spec → component → PR" pipeline. MCP confirmed: Atlassian, Figma.
- 2026-06-09 (v1.0) — Project context created; concept and plan until Sep 04 chosen.
