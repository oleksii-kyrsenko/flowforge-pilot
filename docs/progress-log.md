# FlowForge — Progress log

Append at the top; same entry format as before; see the MAINTENANCE RULE in CLAUDE.md.

- 2026-07-02 — **Engine v3.58→v3.59 — Ф3 engine overhaul (FULL PILOT RESET, decision #30, spec-first from `FLOWFORGE_ENGINE_SPEC.md` v1.4):** implements the ten-item revision (a–j) designed after the T25/T30 source-exhaustion defect class (analyst settling for the primary read and making false absence claims across FF-10/FF-18/FF-22). `.claude/agents/analyst.md` — tool grant (`mcp__figma__download_assets`, `mcp__figma__get_motion_context`, T29), a new mandatory **Coverage inventory** spec section backed by an extended `design-extraction` skill §10 (T30: enumerate ALL top-level nodes of ALL types — frames/groups/sections/canvases, not frames only — with type/name/size, no width-anchoring, before any absence claim; escalated from advisory to a mandatory ATTACHED ARTIFACT), a new hard rule for the intent-vs-ambiguity distinction (build per the mockup as observed + raise the design question in parallel, never in place of a section — an undecided intent never blocks; a genuinely unreachable datum still STOPs, unchanged), and citations to two new skill additions: `behaviour-library-map.md` (T28 — new companion file, seeded carousel→Swiper / continuous marquee→react-fast-marquee / accordion→Radix UI Accordion; expansion under human confirm, never an unrecorded ad-hoc pick) and `SKILL.md` §12 "Breakpoint continuity" (classify every cross-mockup difference as CONTINUOUS or DISCRETE when two or more same-pattern mockups are read). `.claude/settings.json` — `mcp__figma__get_motion_context` added to the Figma allow-list (`download_assets` was already present — the T29 gap was the analyst's own tool grant, not this list). `.claude/commands/spec.md` — new Reuses-dependency gate (spec-threshold: a Reused ticket's spec must exist, need not be built/approved) + fixed the stale T2Z parenthetical claiming the Jira comment needs a pre-post confirm (the live `jira-transition-guard.sh` hook already auto-allows `addCommentToJiraIssue` as class A — confirmed by direct read; the law was already correct, only this command-file line had drifted). `.claude/commands/build.md` — the matching Reuses-gate build-threshold (a Reused ticket must be BUILT — merged on `dev`) + a fluid-CSS-by-default mandate (`clamp()`/`min()`/`max()`/`aspect-ratio`/container queries for continuous properties across the full range; discrete layout-mode switches only at the spec's named threshold; never two fixed `@media` states with the gap unengineered) + a behaviour-library-map consultation step for derived-behaviour implementation picks. `CLAUDE.md` §11 — the `/spec` and `/build` definitions gain the matching Reuses-gate clauses, the Coverage-inventory/breakpoint-classification mandates, and the fluid-CSS default; the "Clean-run & freeze-counter" section gains **(L1.5)** as a new review-time criterion (same nature as the existing L2): Clause A — an absence claim with no attached coverage inventory is a defect; Clause B — an unclassified cross-mockup difference, on any ticket where two-or-more same-pattern mockups were read, is a defect of the same severity. Source: spec-first (`FLOWFORGE_ENGINE_SPEC.md` v1.4 / decision #30 / registry T28, T29, T30, plus items (d)/(f)/(j)). **Not in this PR:** the §4.6 analyst-model fork (Sonnet→Opus) — confirmed by direct grep that no `model:` field exists in any of the three subagent frontmatters, i.e. the T19 base model-routing hypothesis was never implemented as engine config in the first place, so there is nothing for this fork to change; it stays tracked in `FLOWFORGE_ENGINE_SPEC.md` §4.6 + the registry only, same precedent as T20/T21. Also flagged, not fixed (out of the ten-item scope): `mcp__figma__get_libraries` and `mcp__figma__search_design_system` are in the analyst's `tools:` list but not in this allow-list — a pre-existing friction gap, found during inventory, left for a future pass. PR: `chore/f3-engine-overhaul` → `dev` (squash); `.claude/agents/analyst.md` + `.claude/commands/{spec,build}.md` + `.claude/settings.json` + `.claude/skills/design-extraction/SKILL.md` + `.claude/skills/design-extraction/behaviour-library-map.md` (new) + `CLAUDE.md` (§11 + Clean-run section + version line) + this log entry. **Engine edit → freeze-counter stays 0/3 (already reset by the FULL PILOT RESET; no practical cost).**
