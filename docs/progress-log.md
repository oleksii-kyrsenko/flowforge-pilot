# FlowForge — Progress log

Append at the top; same entry format as before; see the MAINTENANCE RULE in CLAUDE.md.

- 2026-07-16 — **v3.89→v3.90, session 18: moved the ticket branch's creation from `/spec`'s post-approval commit node to right after the ticket-lifecycle advance to In Progress (new node 0a; the approval-time commit+push becomes node 0b) — amends decision #35.** Origin: requested directly — once a ticket is In Progress, all its work (including the `/spec` run's own reads/drafting) should happen on its own branch, not only from spec-approval onward. Consequence noted: a `/spec` run that STOPs early (missing frame, unmet Reuses-gate) still leaves an empty branch behind — accepted as the cost of ticket-status/branch symmetry. PR: `.claude/commands/spec.md` (Ticket lifecycle gains step 5 + Commit & push section simplified) + `CLAUDE.md` (Git steps nodes 0a/0b, `/build`'s node-1 bullet, the `/spec` §11 bullet, version line) + this log entry. **Engine edit → freeze-counter stays 0/3** (no open cycle affected — landed before `/spec` FF-9 starts).

- 2026-07-16 — **v3.88→v3.89, session 18: added pause/resume metrics events around every existing STOP point (approval checkpoints, confirm-before-PR/transition gates, anomaly preflights) in `/spec`, `/build`, `/ship`, `/design-fixes`** — enables computing clean agent-work time per stage separate from human wait time, without adding new checkpoints or CSV columns. Origin: requested directly — the developer often doesn't respond to an approval checkpoint immediately, and the existing start/done interval silently absorbed that wait. Engine edit → freeze-counter stays 0/3 (no open cycle affected — landed before `/spec` FF-9 starts).

- 2026-07-16 — **v3.87→v3.88, session 18: branch-naming convention corrected `flowforge/<KEY>-<slug>` → `feat/<KEY>-<slug>`** — the project identity already lives in the Jira key, the prefix should be the commit-type convention, not the pipeline's own name. Origin: caught immediately after #47 shipped the old naming; fixed same-session as a follow-up engine PR (not an amendment, since #47 was already merged). PR: `CLAUDE.md` (Git steps Node 0/1 + `/build`/`/ship` bullets in §11 + version line) + `.claude/commands/spec.md` (Commit & push section) + `.claude/commands/build.md` (Branch bullet) + this log entry. **Engine edit → freeze-counter stays 0/3.**

- 2026-07-16 — **v3.86→v3.87, session 18: gave `/spec` its own commit+push node (Node 0) — spec artifacts commit and push to the ticket branch immediately on approval, `/build` checks the branch out instead of recreating it.** Resolves the CLAUDE.md "Metric-row binding" deferred question. Origin: insurance against `/spec` and `/build` being run by different developers, requested directly (session 18). Retroactively applied to FF-8 (see separate branch push, same session) — this engine edit lands mid-FF-8's open cycle; per decision #34's precedent (git-mechanics-only edits don't touch spec content/values), FF-8's spec-level clean status is unaffected. PR: `CLAUDE.md` (Git steps section + version line) + `.claude/commands/spec.md` (new Commit & push section) + `.claude/commands/build.md` (branch-checkout + commit-scope wording) + this log entry. **Engine edit → freeze-counter stays 0/3.**

- 2026-07-16 — **v3.85→v3.86, session 18: deferred `/spec`'s Jira-comment posting of the full spec text to an optional post-window phase (not MVP/Target/Stretch-required); `docs/specs/<KEY>.md` remains the sole spec artifact; ticket lifecycle transitions/assignee claim unaffected; hook's `addCommentToJiraIssue` allow-rule left in place, dormant.** Origin: identified as unnecessary overhead at this stage during the FF-8 `/spec` audit (session 18). Note: this engine edit landed mid-FF-8's open cycle; FF-8's cycle is counted CLEAN by a one-time explicit human exception (see FLOWFORGE_CHAT_CONTEXT.md decision #34) — Clean-run L1 point 1 itself is NOT amended. PR: `CLAUDE.md` (§4 primary scenario + ambition levels + `/spec` bullet + hook description + version line) + `.claude/commands/spec.md` (Do section + Then STOP section) + this log entry. **Engine edit → freeze-counter stays 0/3.**

- 2026-07-13 — **v3.84→v3.85, session 18: added §11 "Component conventions" (universal/polymorphic variant+asChild standard, project-wide, ticket-inherited); scoped design-extraction §10 coverage by ticket nature (component/primitive → design-system variant set + own breakpoint frames; route/page & baseline → full page enumeration, T30 hardening intact); mirrored in the /spec bullet + analyst.md.** Origin: FF-8 /spec run (session 18) over-swept the document for a component ticket and falsely asserted "exactly 2 top-level pages" (19 exist, controller-verified) — registry T40. Engine edit → freeze-counter stays 0/3.

- 2026-07-12 — **First `/baseline` build on the "Components" source (the fresh, fully blind re-run owed since v3.82/v3.83) — token map, gradients, icon states, and one real authoring inconsistency captured; PR pending.** Task output, not an engine edit. Ran the full pipeline against https://www.figma.com/design/JFKSRWAuZTSR4nUMNKe0Gn/Components?node-id=0-1&p=f&m=dev: single page, 18 top-level component groups (Header, Button/BOOK-IT/Read-More, Accordion "Open" ×3 states, Menu Item ×4 property variants, Instagram icon, pagination, text Input ×5 states, Item car-listing card, eye/eye-off toggle, Logo build-in animation, headlight photography, Favicon). All six mechanical gates passed clean after fix-up (citations, completeness, deep-read, near-match scoping, verify-node tags all blocking-clean; sibling-coverage's 8 WARN groups individually dispositioned in the draft rather than left silent). **Confirmed independently, not assumed:** the Instagram-icon opacity trap (40%/100%/80%) the design-extraction skill documents as this file's own historical incident. **New genuine find:** the "BOOK IT" link-button's arrow icon in its Press state uses the Normal gradient instead of `Gradient/Press` (its adjacent text correctly uses Press) — recorded verbatim, opened as Design Question Q2 rather than silently "corrected." Also found: several flattened `border-[#hex]` reads (accordion Normal/Active borders, header hairline) were actually gradients on deeper inspection, not flat colors. **Primary font: Poppins**, auto-confirmed (100% census, zero competing families, per skill §8). **Applied** on branch `chore/token-baseline`: `docs/design-tokens.md` filled (colors, gradients, shadows, strokes, icons, typography, spacing, corner-radii/breakpoint/motion coverage notes, pending list); `docs/design-questions.md` gets Q1 (Menu Item Hover text-brightness inconsistency) and Q2 (BOOK-IT Press-icon gradient mismatch); `src/app/globals.css` `@theme` block gets `--color-primary`, `--color-error`, `--color-nav-underline`, the three gradient-stop pairs (`gradient-{,hover-,press-}{from,to}`), `--shadow-button`, and `--font-sans` repointed to Poppins. **Scaffold audit (CLAUDE.md §11) applied:** removed the inherited `prefers-color-scheme: dark` toggle and its light-mode `:root` values — the confirmed theme is dark-only (`Background` = `#080C0F` everywhere in the source, no light variant exists) — base `--background`/`--foreground` now wired directly to the confirmed tokens. `src/app/layout.tsx` swapped `Geist`/`Geist_Mono` for `Poppins` (weights 300/500/600/700) via `next/font/google`, set on `<body>`; `Geist_Mono` removed entirely (zero usages anywhere in `src/`, no confirmed role in the design source). PR not yet opened — pending human review of the applied diff before `gh pr create` (per explicit instruction on this run). — `/baseline` now dumps its checkpoint draft to disk on every run, so the human and the read-only controller can both read the agent's result directly (not only from chat).** New behavior in the CHECKPOINT step: immediately before presenting, write the full checkpoint content to the gitignored scratch file `.claude/tmp/baseline-draft.md` — truncate/replace every run, never append, same discipline `baseline-raw-transcript.txt` already follows. Applies on ALL run branches, including the RE-RUN "No changes against the approved map" branch, so a stale prior dump can never be mistaken for the current run's outcome. This is a pre-approval scratch artifact, not a canon write — canon still only enters the repo via the APPLY phase's PR to `docs/design-tokens.md`. The checkpoint report presented in chat must cite the `.claude/tmp/baseline-draft.md` path. **`.claude/commands/baseline.md`** updated in the CHECKPOINT section with the dump/overwrite/all-branches mechanism and the path-citation requirement. **`CLAUDE.md`'s `/baseline` bullet (§11)** gets one line pointing at the command file for the mechanism, keeping single-source discipline. **Gitignore coverage confirmed, no change needed** — `.claude/tmp/` is already a directory-wide ignore rule; `git check-ignore -v .claude/tmp/baseline-draft.md` confirms the match. PR: `.claude/commands/baseline.md` + `CLAUDE.md` (the §11 bullet + version line) + this log entry. **Engine edit → freeze-counter stays 0/3.\*\*

- 2026-07-12 — **Engine v3.82→v3.83 — a live `/baseline` dry-run on the "Components" source surfaced two real extraction errors that survived all five existing gates, and this cycle closes the four gaps that let them through.** Errors found (both confirmed by direct re-check, draft discarded — not patched): (a) three placed instances of the same Instagram icon carried opacity 40%/100%/80%; only one was ever independently deep-read, one was inferred purely from an absent Tailwind class in flattened code, and the third's real 80% value sat unread in already-fetched data because the follow-up `grep` only checked `fill=`, never `opacity=`; (b) an accordion Active-state border's true gradient value was incidentally present in a transcript response (via the skill §9 parent/child leak) but got folded into vague, untagged prose instead of its own `[Verify-node:]`-backed claim, so gate 5 had nothing to check — the value was correct, in transcript, and still uncaught. **Four fixes land together:** **(1) new gate 6** (`.claude/scripts/validate-checkpoint-sibling-coverage.sh`, WARN-tier, never blocks) — groups node-ids sharing a `data-name` in `get_design_context` output and flags any group with partial/zero individual `download_assets`/`get_variable_defs` coverage; deliberately blunt (cannot tell a state-varying group from a decorative repeat, so it over-flags on purpose per skill §13/§5's rate-limit and dedup constraints). Verified against this session's real transcript: correctly flagged the Instagram group plus several previously-unknown gaps (Send/Read-More button instances were never independently deep-read, only inferred via a shared style name). **(2) §9 "coincidental match" section widened in place** (not a new section — single-source discipline) from paint/color-only scope to any independently-overridable property, opacity and blend-mode now named explicitly, with the Instagram case added as a second worked example beside the existing gradient-border one; the `[Verify-node:]` tag mandate paragraph is unchanged. **(3) `figma-transcript-capture.sh` now auto-fetches the real SVG body** for every `download_assets` call (size-capped at ~1MB — the tool's own JSON response only ever carried an export URL, never the paint/opacity data inside it, so gate 2 had no ground truth for anything only visible in a downloaded body; confirmed via an isolated synthetic test that gate 2's EXISTING regex, unchanged, now catches a body-only hex and opacity value it structurally could not see before). **(4) a new call-time ancestor-leakage nudge** in the same hook: compares an auto-fetched SVG's declared `width`/`height` against its internal coordinate extents and, on a sharp divergence, surfaces a genuine `PostToolUse` `hookSpecificOutput.additionalContext` advisory (verified against the exact real node that caused finding (b) — fired correctly; verified silent on a self-contained icon export and on a size-capped photo export). **§14 renumbered** five→six gates; its closing "Scope boundary" paragraph now also names the one residual class no gate can close — a value never written down in any claim-shaped form — as a human/controller full-re-read responsibility, explicit that "all six gates pass" is not "every per-instance override was checked." **`/baseline` and `/spec` command files updated** from "five gates" to "six gates." PR: `.claude/skills/design-extraction/SKILL.md` (§9 + §14) + `.claude/scripts/validate-checkpoint-sibling-coverage.sh` (new) + `.claude/hooks/figma-transcript-capture.sh` + `.claude/commands/baseline.md` + `.claude/commands/spec.md` + `CLAUDE.md` (version line) + this log entry. **The `/baseline` checkpoint draft from this investigation stays DISCARDED, not patched** with the corrected values found along the way — a fresh, fully blind re-run on the same "Components" source is the next step, a separate manual action not run as part of this fix. **Engine edit → freeze-counter stays 0/3.**

- 2026-07-11 — **Engine v3.81→v3.82 — Part B of the v3.81 fix lands: a 5th mechanical checkpoint gate (`validate-checkpoint-verify-node-tags.sh`) plus the mandatory `[Verify-node: X:Y]` tag requirement that makes its block tier reachable.** v3.81 documented the parent-to-child descendant-export trap in skill §9 but never required authors to actually tag a resolved-flattened-paint claim — without that requirement every claim would land in gate 5's non-blocking warn bucket and the block tier could never fire; the controller caught this gap before implementation. **§9 addition:** a new mandatory-tagging paragraph, same weight as §4's existing Role/Component mandate ("not optional formatting — it is the evidence a mechanical gate checks") — any claim resolving a flattened/ambiguous paint via `get_variable_defs`/`download_assets` must carry `[Verify-node: X:Y]` naming the EXACT node-id the calls were made on; tagging a child/descendant used only as an incidental data source does not count. **§14 updated** to five gates, with gate 5 documented (block: tagged id has no matching call in the transcript; warn: trigger wording + a node-id citation but no tag at all — non-blocking, a nudge only). **Gate script** (`.claude/scripts/validate-checkpoint-verify-node-tags.sh`): extracts every `[Verify-node: X:Y]` tag from the draft, cross-checks each against `get_variable_defs`/`download_assets` `tool_input` blocks in the raw transcript for that exact `"nodeId"` — no parent/child substitution accepted, matching the exact scoping decision from v3.81. Untagged lines matching verification-style trigger wording (`confirmed`, `verified`, `svg-confirmed`, `FALSE FLATTENING`, `caught via svg`, etc.) plus a node-id citation warn without blocking. **Verified against four synthetic cases** (same rigor as the Gate 2 regex verification earlier this cycle), each isolated and re-run in combination: (1) tagged + backed → silent pass, exit 0; (2) tagged + never-called node → block, exit 1; (3) tagged + parent/child trap reproduced exactly (only the child node had a transcript call) → block, exit 1; (4) untagged prose claim → warn printed, exit 0 (non-blocking). A fifth edge case (no tags, no trigger words) confirmed a clean silent pass with no false positives. `bash -n` syntax-checked against all five gate scripts. **`/baseline` and `/spec` command files updated** from "four gates" to "five gates," noting gate 5's warn tier doesn't block by itself. PR: `.claude/skills/design-extraction/SKILL.md` (§9 + §14) + `.claude/scripts/validate-checkpoint-verify-node-tags.sh` (new) + `.claude/commands/baseline.md` + `.claude/commands/spec.md` + `CLAUDE.md` (version line) + this log entry. **A fresh, fully blind `/baseline` re-run is still owed** on the "Components" source (v3.81's discarded draft) — not run as part of this fix, stays a separate explicit action. **Engine edit → freeze-counter stays 0/3** (already reset; no additional cost).

- 2026-07-11 — **Engine v3.80→v3.81 — design-extraction skill §9 gains a parent/child descendant-export sub-rule, after a controller cross-check caught an unverified node-identity claim during a `/baseline` dry run on the "Components" Figma file (Elite Fleet Group).** During extraction, the draft checkpoint labeled the Accordion "Open" Normal-state border (node `2:94`) "svg-confirmed" after `get_design_context` had flattened it to a solid `rgba(255,255,255,0.2)`. The actual verification data had leaked in from a `download_assets` call made on a CHILD node (`2:99`, the chevron-down icon) — Figma's SVG export includes ancestor positioning/paint context, so the child's export happened to also carry the parent frame's own border gradient, and that incidental payload was read and attributed to `2:94` without ever calling `get_variable_defs`/`download_assets` with `2:94`'s own node-id. The controller found this by diffing the checkpoint draft's claims against `.claude/tmp/baseline-raw-transcript.txt` and confirming no tool call in the transcript ever targeted `2:94` directly. All four existing mechanical gates (citation/completeness/deep-read/near-match-scoping) passed on the flawed draft — none of them check that a "verified"-style claim's node-id matches the node-id actually named in a `get_variable_defs`/`download_assets` tool call. **Two-part fix, sequenced:** **Part A (this PR):** added an explicit sub-rule to skill §9's coincidental-hex-match section distinguishing this parent-child leakage trap from the already-documented sibling-state trap (Hover's confirmed value doesn't carry over to Press) — a descendant's export is never a substitute for calling the tool on the claimed node's own id, even when the payload is factually accurate, because the parent is always independently addressable. **Part B (investigated and proposed this session, NOT yet implemented — awaiting go-ahead):** a new dedicated gate to mechanically check that any verification-claim in the checkpoint draft (e.g. "svg-confirmed", "FALSE FLATTENING caught via svg") names a node-id backed by an actual `get_variable_defs`/`download_assets` call on that EXACT id in the transcript — proposed as a structured `[Verify-node: X:Y]` tag (mirroring the existing Gate 4 `[Role:.., Component:..]` convention) checked by exact-id match only (no parent/child leniency — that would reopen the exact hole Part A just closed), blocking like Gates 1-4, plus a non-blocking prose-trigger warn pass (words like "confirmed"/"verified" + a node-id citation but no tag) to nudge adoption of the tag without hard-stopping on phrasing variance alone. **Re-verification result: the flagged value itself was correct** — direct `get_variable_defs`(`2:94`) returned `{}` (unbound) and direct `download_assets`(`2:94`) + curl confirmed the same gradient (`white 20%→0%` opacity, top-to-bottom) already in the draft; only the verification-claim provenance was wrong, not the data. **Per the clean-run standard (T25/T30 precedent extended to `/baseline`), this run's checkpoint draft was discarded rather than carried forward corrected** — a controller-caught unverified claim disqualifies the run even if patched within the same session. A fresh, fully blind `/baseline` re-run on this same source is needed once Part B lands; not scheduled as part of this fix — stays a separate, explicit manual action. PR: `.claude/skills/design-extraction/SKILL.md` (§9) + `CLAUDE.md` (version line) + this log entry. **Engine edit → freeze-counter stays 0/3** (already reset; no additional cost).

- 2026-07-10 — **Engine v3.79→v3.80 — 10 provenance-leak instances found across two controller audits and fixed: shipped engine text must stand alone for a template adopter who never saw this pilot, with no session/task-id numbers and no citation of a controller-side working document that doesn't ship.** Two audit rounds, both investigation-first (report before any edit), both fully applied this PR. **Round 1 — numeric session/task-registry-id citations used as narrative precedent inside otherwise-general rule text (4 instances, 2 caught by the controller after this session's own prior fix had just introduced a near-identical defect — proof manual diligence alone wasn't sufficient):** `.claude/hooks/figma-transcript-capture.sh` header comment dropped "(session 16 / T38-T39: a model-authored placeholder summary silently broke Gate 3's detection for node 2:51)" — the preceding sentence ("removes the model's opportunity to paraphrase or omit a tool response before logging it") is already a complete, self-justifying rule without the incident citation. `.claude/scripts/validate-checkpoint-deep-read.sh`'s boilerplate-sentence comment dropped "confirmed session-16 dry-run + T39 hook capture" — the claim "present verbatim on every get_design_context call" stands alone as a factual statement regardless of who confirmed it. `.claude/skills/design-extraction/SKILL.md` §13 dropped "(session 11, `/baseline`)" from the concurrency/rate-limit rule — "has been observed to exhaust the shared quota within a few calls" needs no session number to be a complete empirical claim. `CLAUDE.md`'s Metric-row binding bullet (§11, Git steps) dropped "(registry T2Y)" and the entire trailing "Reference cycle: FF-8; FF-9's grouping was an anomalous late-batch artifact" sentence — both cited artifacts (an external registry, this pilot's own Jira ticket numbers) meaningless to an adopter, and the rule ("a deferred engine question, not current behavior") is complete without them. **Round 2 — citations of "GUIDE Prompt/Appendix N," a controller-side working document (FLOWFORGE_GUIDE.md) that is never part of the extracted template (6 instances, all in general/law text; a 7th candidate at CLAUDE.md §8 "Setup steps: GUIDE Appendix A..." was deliberately left untouched — flagged to the controller as a judgment call since §8 is this file's own designated project-data slot, not general law, and the controller confirmed: exclude):** dropped the bare "(GUIDE Prompt N)" parenthetical from CLAUDE.md's Code style & pre-commit section ("GUIDE Prompt 2"), Skills section's design-extraction description ("GUIDE Prompt B"), and both Metrics & logging bullets ("GUIDE Prompt 6", "GUIDE Prompt 7" — the latter required also closing the now-empty `Weekly ():` parens left by a bare drop, so it reads "Weekly:"); dropped the same parenthetical from design-extraction/SKILL.md's own frontmatter `description:` field. One instance needed more than a bare drop: CLAUDE.md's Project-structure directory-tree comment for `docs/design-questions.md` read "created by Prompt B baseline" — dropping only "Prompt B" would leave bare "created by baseline," a real ambiguity risk (an unfamiliar reader could parse "baseline" as the ordinary English word "from the start" rather than the `/baseline` command, especially since the exact same line already uses slash-prefixed command names — `/spec`, `/review`, `/design-fixes` — for its other three commands); resolved to "created by /baseline," using the command's own already-established name from elsewhere in this same file, not fabricated content. Every one of the 10 rewrites was re-verified against its surrounding sentence for completeness/correctness before finalizing, per the controller's explicit instruction not to leave a dangling or now-incomplete claim. PR: `.claude/hooks/figma-transcript-capture.sh` + `.claude/scripts/validate-checkpoint-deep-read.sh` + `.claude/skills/design-extraction/SKILL.md` (§13 + frontmatter) + `CLAUDE.md` (5 bullets across §11, version line) + this log entry. Both audits were investigation-only turns (report first, wait for go-ahead) before this fix-everything turn — no file was touched until explicit approval. **Engine edit → freeze-counter stays 0/3** (already reset by the Ф3 FULL PILOT RESET; no additional cost).

- 2026-07-10 — **Engine v3.78→v3.79 — two logic defects found during controller verification of a `/baseline` dry-run on the Components file (session 17), both fixed at the source (gate script + skill methodology), draft checkpoint left untouched.** The controller cross-checked the presented `/baseline` checkpoint against `.claude/tmp/baseline-raw-transcript.txt` and live Figma reads and surfaced two gaps. **(1) Gate 2 (`validate-checkpoint-completeness.sh`) regex blind spot.** `extract_colors()`'s `COLOR_PATTERN` matched hex/rgba/opacity-attribute values only — it had no coverage for Tailwind's bare color-keyword utilities (`text-white`, `from-white`, `bg-[transparent]`, etc.), even though these are genuine, distinct color values that appear directly in `get_design_context`'s returned className strings (confirmed present in this run's own transcript: `text-white` ×35, `from-white` ×8, `text-[transparent]` ×14 — none of which the old pattern matched at all). **Fix:** added a `KEYWORD_COLOR_PATTERN` alternation covering the white/black/transparent keywords across the standard color-bearing Tailwind prefixes (`text`, `bg`, `border`, `from`, `via`, `to`, `fill`, `stroke`, `ring`, `divide`, `outline`, `accent`, `caret`, `decoration`, `placeholder`, `shadow`), in both bare (`text-white`) and bracketed (`text-[transparent]`) form — written as two full alternatives rather than independently-optional brackets, so a malformed one-sided bracket can never match. **Verified, not just written:** confirmed the OLD pattern misses `text-white`/`from-white`/`text-[transparent]` in the real transcript (zero matches); confirmed the NEW pattern catches all three; probed 9 known non-color Tailwind classes present in the same transcript (`whitespace-nowrap`, `text-sm`, `items-start`, `content-stretch`, `border-solid`, `shadow-`, `rounded-`, `overflow-clip`, `flex-col`) — zero false positives on any of them; ran the full gate script end-to-end against synthetic raw/draft file pairs — correctly FAILS when the bare-keyword colors are absent from the draft, correctly PASSES once they're recorded. **(2) Deep-read discipline gap (Gate 3 + design-extraction skill §9/§10) — a passed `download_assets` call does not prove the export was actually read.** Confirmed directly: nowhere in this run's 1184-line raw transcript does any actual SVG payload (`stop-color`, `<svg`, etc.) appear — the hook only captures `get_design_context`/`download_assets`/`get_variable_defs` tool*response JSON, and `download_assets`'s own tool_response is just an export-URL metadata wrapper; the real SVG bytes only ever reach the model via a separate, unhooked `curl` Bash call. This means a flattened border/fill that coincidentally renders the same hex as an already-established token could be recorded as "confirmed, same token" purely from a passed Gate-3 call, without the SVG content ever having been fetched or read — and without inferring identity from a sibling state's already-confirmed pattern being flagged as insufficient. This is a methodology gap the mechanical gate cannot itself close (it only proves the tool was \_called*, never what its payload contained, and `curl` isn't a hookable tool at all) — so the fix lives in the design-extraction skill, not the gate script. **Fix:** added an explicit sub-step to skill §9, right after the existing curl-over-WebFetch rule: when `get_design_context` flattens a paint to a solid hex matching an existing token, (a) run `get_variable_defs` on that exact node first — a bound variable/style settles identity outright; (b) if unbound, the `download_assets` export for that SAME node MUST actually be curled and its real paint data read before the claim is written — a sibling state's already-confirmed pattern is explicitly named as NOT sufficient evidence on its own. Cited this session's own Accordion Normal-border finding (flattened to a flat `rgba(255,255,255,0.2)` by `get_design_context`, but actually a top-to-bottom gradient in the real SVG) as the in-repo precedent proving the risk is real, not hypothetical. PR: `.claude/scripts/validate-checkpoint-completeness.sh` (`KEYWORD_COLOR_PATTERN` added) + `.claude/skills/design-extraction/SKILL.md` (§9 gains the coincidental-hex-match sub-step) + `CLAUDE.md` (version line only) + this log entry. Per the controller's explicit instruction, the `/baseline` dry-run's own draft checkpoint was NOT modified and `/baseline` was NOT re-run as part of this fix — both are separate, manually-invoked steps. **Engine edit → freeze-counter stays 0/3** (already reset by the Ф3 FULL PILOT RESET; no additional cost).

- 2026-07-10 — **Engine v3.77→v3.78 — `/spec` extension of T39's hook-driven raw-transcript capture, after an empirical test overturned this session's own earlier GitHub-issue-based conclusion.** Investigating whether T39's `figma-transcript-capture.sh` hook also closes `/spec`'s parallel gap (the `analyst` subagent has no `Write`/`Bash`, so `spec.md`'s existing "require the analyst to write its raw tool-call transcript... before returning control" instruction was already unactionable, independent of hooks), the first-pass answer relied on `anthropics/claude-code` issue #34692 ("PreToolUse/PostToolUse hooks do not fire for subagent (Agent tool) tool calls," closed as "not planned") to conclude the merged hook could never fire for `analyst`'s internal Figma calls, since `analyst` is a Task/Agent-tool subagent. **That conclusion was wrong, and was corrected the same session by direct empirical test, not by further reading:** invoked `Agent({subagent_type: "analyst"})` with a prompt to make exactly one `mcp__figma__get_design_context` call (node 2:167) and reply "done"; read `.claude/tmp/baseline-raw-transcript.txt` before and after — the file gained a new entry, header `=== mcp__figma__get_design_context tool_input={"nodeId":"2:167",...} ===`, containing the complete raw response (including an embedded base64 screenshot block this particular call happened to include). **The hook fires for subagent-internal tool calls in this Claude Code version, contradicting the GitHub issue.** No text/config change was made on the strength of the issue thread alone — the issue's own closure ("not planned," filed by a third-party reporter, no visible maintainer comment in the fetched content) was correctly treated as inconclusive per this session's own discipline, and the actual, decisive answer came from testing this repo's own environment directly. Lesson recorded for future sessions: an external GitHub issue — even a specific, well-reproduced, closed one — is not a substitute for a five-minute empirical check against the actual running environment when the check is this cheap to perform. **Fix shipped on the strength of the corrected finding:** `spec.md`'s "Mechanical verification gates" section is rewritten to (i) describe the raw Figma-tool-response half as hook-driven — the same `.claude/hooks/figma-transcript-capture.sh` fires for `analyst`'s calls with zero `Write`/`Bash` needed, appending to the same `.claude/tmp/baseline-raw-transcript.txt` `/baseline` uses — and (ii) add `/spec`'s own truncate-at-start step, since the shared fixed path is a REAL, already-demonstrated cross-command leak risk, not a hypothetical one (this session's own dry-run for T39 left exactly such a leftover entry in that file, which the subagent test above then appended onto without cleanup — a live example of the defect a truncation step exists to prevent). Separately, (iii) the **draft spec text** (the analyst's own synthesized prose, as opposed to raw Figma tool responses) is NOT covered by the hook — it was never a Figma tool response in the first place — and still crosses the subagent boundary only via the analyst's single final returned message (per the Agent tool's own documented behavior); the rewritten section now says so explicitly and has the ORCHESTRATOR write that returned text to a new scratch file, `.claude/tmp/spec-draft-<JIRA-KEY>.md`, immediately after the Task returns — correcting the previous wording's claim that the analyst does this write itself "before returning control," which was never true regardless of the hook question. Skill §14's general prerequisite text is UNCHANGED, same reasoning as the `/baseline` fix — it describes the required outcome, not a specific mechanism, and this PR only changes how `/spec` achieves it. **Gate 2 silent-crash defect found during this PR's own targeted dry-run, root-caused and fixed in the same PR:** running the first dry-run (truncate → single `analyst` Figma call → orchestrator writes the analyst's returned placeholder text to a scratch draft → all four skill §14 gates) surfaced that `validate-checkpoint-completeness.sh` produced NO output at all and exited 1 — not a proper "MISSING FROM DRAFT" report, a silent crash. Traced with `bash -x`: the placeholder draft text contains zero color/opacity matches, so `extract_colors()`'s `grep -oE ... | sort -u` pipeline's `grep` stage exits 1 (no match), `pipefail` propagates that as the pipeline's status, and since the pipeline is the right-hand side of `DRAFT_COLORS=$(...)`, the script's own `set -euo pipefail` kills it right there — before the comparison loop or either designed message (`MISSING FROM DRAFT:` / `OK`) ever runs. Pre-existing in the gate script, unrelated to anything this PR's own changes touch — it never surfaced during `/baseline`'s dry-runs because a real design-token draft always contains at least some color values; a genuinely non-UI `/spec` ticket's draft (or this synthetic placeholder) is exactly the input shape that trips it. **Fix:** one line, `|| true` appended to `extract_colors()`'s own pipeline (mirroring the identical guard `validate-checkpoint-deep-read.sh` already uses on its `DEEP_READ_NODES`/`DEEP_READ_NODES_ALT` pipelines) — an empty match set is a legitimate state, not an error; the existing downstream comparison loop already handles an empty `$RAW_COLORS`/`$DRAFT_COLORS` correctly once the script survives to reach it. Verified in isolation before applying (a scratch copy of the function, run against both an empty-match file and a has-matches file) and again in the real re-run below: a genuine dropped-value case (raw has colors, draft doesn't) still correctly reports `MISSING FROM DRAFT:` and exits 1 — the fix restores the gate's real detection, it doesn't weaken it. **Audited the other three gate scripts for the same shape** (`validate-checkpoint-citations.sh`, `validate-checkpoint-near-match-scoping.sh`) — neither has it: every `grep` in both files is either the condition of an `if` (exempt from `set -e` by bash's own semantics) or feeds a process-substitution `while` loop (also exempt, and separately confirmed empirically that a no-match `grep` there doesn't propagate); the one command-substitution-into-assignment pattern in `near-match-scoping.sh` (lines 26–29) pipes through `sed`/`xargs`, neither of which treats "no match" as a failure the way `grep` does, and only ever runs once two non-empty tags already exist. No changes proposed or made to either file. **Dry-run evidence (Phase 2, targeted at the new plumbing only — no real Jira ticket touched):** see PR description for the truncate → single hook-capture → Task-return → orchestrator-write → four-gates-clean chain, run end to end twice (once exposing the Gate 2 crash, once confirming the fix) against synthetic test content. PR: `.claude/commands/spec.md` (Mechanical verification gates section rewritten) + `.claude/scripts/validate-checkpoint-completeness.sh` (`extract_colors()` gains `|| true`) + `CLAUDE.md` (version line only) + this log entry. **Engine edit → freeze-counter stays 0/3** (already reset by the Ф3 FULL PILOT RESET; no additional cost).

- 2026-07-10 — **Engine v3.76→v3.77 — hook-driven Figma raw-transcript capture for `/baseline` (registry T39), closing the model-paraphrase gap found investigating T38.** Session-16's `/baseline` checkpoint on the Components file logged a placeholder summary ("[Full React/Tailwind code... see tool output above]") instead of the verbatim `get_design_context(2:51)` response into the scratch transcript Gates 1–4 depend on — Gate 3 (deep-read) passed by construction because it had nothing to flag, not because anything was verified. **T38 investigation:** confirmed this was a pure compliance failure, not a rule-text gap — design-extraction skill §9 already unambiguously forbids deferring an icon's styled-property color under any framing ("no exception for... STYLED PROPERTIES"), and skill §14's raw-transcript prerequisite already says "not a summary, the actual returned text." No skill-text change made. **T39 investigation (two rounds):** established that logging was 100% model-driven prose compliance with zero automatic capture (`.claude/settings.json` had no `PostToolUse` hook on any `mcp__figma__*` tool), so no mechanical gate downstream can reliably prove "verbatim" after the fact — the ground truth is gone from the transcript file by the time any gate runs; a placeholder-phrase or boilerplate-footer heuristic gate was assessed and rejected as unreliable (rewards the one honest disclosure, misses every silent paraphrase — confirmed empirically: re-auditing session-16's own transcript found the paraphrase was NOT isolated to node 2:51, it was the file's general condition — only four short `get_variable_defs` JSON blobs were genuinely verbatim). Round two confirmed Claude Code's own session JSONL (`~/.claude/projects/<project>/<session-id>.jsonl`, documented at code.claude.com/docs/en/sessions) already holds the true raw `tool_result` independent of anything the model writes — demonstrated directly against this session's own log — but is explicitly documented as an internal, version-unstable format not meant for direct script parsing; resolved to pursue hook-payload capture instead, per code.claude.com/docs/en/hooks confirming `tool_response` (array of `{type,text}` blocks) is a real PostToolUse stdin field. **Fix shipped (draft approved this session, applied verbatim, Phase 2 dry-run passed before this PR — see below):** a new PostToolUse hook, `.claude/hooks/figma-transcript-capture.sh` (matcher `mcp__figma__get_design_context|mcp__figma__download_assets|mcp__figma__get_variable_defs` — confirmed against Gate 2/Gate 3's actual scan logic that `get_metadata`/`get_screenshot`/`get_motion_context` carry none of the patterns either gate checks, so they're deliberately excluded, not an oversight), appends the harness's own `tool_response` verbatim to `.claude/tmp/baseline-raw-transcript.txt` — the model never chooses what gets written. `baseline.md`'s "Raw-transcript logging" bullet now describes this hook-driven mechanism and adds a truncate-at-command-start step so a prior run's entries can't leak into the current checkpoint's gate check. `.gitignore` gains `.claude/tmp/` (the capture path moved in-repo since a hook process has no reliable pointer to the model's own ephemeral scratchpad directory). Skill §14 is UNCHANGED (deliberately out of scope — this fix is `/baseline`-only; `/spec` unaffected). **Gate 3 false positive found and fixed during the Phase 2 dry-run itself:** the first live dry-run call (`get_design_context` on node 2:167) proved the hook captures the complete, byte-for-byte raw `tool_response` (verified against the live call, including the dynamic per-call asset UUID and every boilerplate paragraph) — but running `validate-checkpoint-deep-read.sh` against that real hook-authored file surfaced a latent gate defect that had never been visible before: the MCP server's own fixed boilerplate footer contains two literal doc-example fragments — `data-node-id="1:2"` and `<img src={image} />` — that Gate 3's naive line-scanning awk mistook for a real flattened-node pair, spuriously flagging node "1:2" on every single run. This was invisible before this fix because hand-summarized transcripts never included the boilerplate verbatim in the first place. Root-caused precisely (re-read the actual dry-run file, confirmed a real per-node Component-descriptions block — `## chevron-down` / `**Node ID:** 2:36` — legitimately sits BETWEEN the two boilerplate sentences, ruling out a naive range-delete) and fixed with a two-line `grep -vF` prefilter anchored on the full, exact fixed-sentence text (not the bare tokens, and not a between-range) ahead of the existing awk pipeline — `DEEP_READ_NODES`/the `download_assets` cross-check is untouched. Re-ran Gate 3 against the same dry-run file post-fix: node "1:2" no longer flagged; node "2:172" (the real chevron-down icon, genuinely never deep-read in this dry-run) still correctly flagged — confirmed the fix removes only the false positive, not the gate's real detection. PR: `.claude/hooks/figma-transcript-capture.sh` (new) + `.claude/settings.json` (new `PostToolUse` entry) + `.gitignore` (`.claude/tmp/` line) + `.claude/commands/baseline.md` (Raw-transcript logging bullet replaced) + `.claude/scripts/validate-checkpoint-deep-read.sh` (FLATTENED_NODES boilerplate-exclusion prefilter) + `CLAUDE.md` (version line only) + this log entry. **Dry-run evidence (Phase 2, before opening the PR):** see PR description for the verbatim before/after Gate 3 output. **Engine edit → freeze-counter stays 0/3** (already reset by the Ф3 FULL PILOT RESET; no additional cost).

- 2026-07-09 — **Engine v3.75→v3.76 — `/build` wired to skill §14's Gate 3 (deep-read) for icon-geometry extraction; a proposed `analyst` curl-grant was investigated and deferred, not implemented.** Two related items were reviewed together (shared root cause: design-extraction tool-access and gate-wiring gaps), but only one shipped. **(i) Shipped — `/build`'s icon-geometry gap.** Design-extraction skill §14's four mechanical gates are meant to run wherever a checkpoint draft is produced and `Bash` is available; `/build`'s own orchestrator already has `Bash` (it runs lint/typecheck/test:run there) and performs the exact raster-vs-vector pattern Gate 3 (deep-read) checks — but `.claude/commands/build.md`'s "Icons / vector assets" bullet had zero reference to skill §14 or any gate, relying on a prose-only STOP rule (hard rule 5) alone. This was an inconsistency with the skill's own stated intent, not a deliberate exclusion (unlike `/design-fixes`'s exclusion — see (iii) below, which IS deliberate and reasoned). Fix: the icon bullet now requires logging the raw icon-tool-call transcript and running Gate 3 (`validate-checkpoint-deep-read.sh`) against it before accepting either final geometry or a STOP conclusion. Gates 1/2/4 were deliberately NOT wired — `/build` produces no color/token checkpoint draft (color/opacity extraction happens at `/baseline`/`/spec` time per skill §9), so citation/completeness/near-match-scoping have nothing to check against. **(ii) Investigated, NOT shipped — a proposed `Bash(curl ...)` grant for `analyst`.** A same-day proposal to grant `analyst` a settings.json-scoped `Bash(curl https://www.figma.com/api/mcp/asset/*)` rule (letting `/spec`/`/design-fixes` read raw SVG bytes behind a `download_assets` export URL when no named style/variable is exposed) was checked against the registry entries it claimed to build on — **T34 turned out to be a different, unrelated tail** (`/build`'s missing paint-value STOP, not an analyst-access gap), and **T36** (the real match — a narrow-fetch-tool alternative to a full analyst-`Bash` grant, deferred session 11) explicitly calls its own fix "a genuine engineering task... worth its own dedicated session, not a rider on an unrelated day's changes." Two rounds of Claude Code permissions-documentation research then showed the proposed settings.json pattern is not the real technical boundary it looks like: command CHAINING is safe (Claude Code is shell-operator-aware and requires each `;`/`&&`/`||`/`|`-separated sub-command to independently match an allow rule), but Anthropic's own docs call argument-constraining Bash patterns "fragile" and name the exact failure mode — a trailing `*` after a fixed URL prefix is swallowed whole by the wildcard, so `curl https://www.figma.com/api/mcp/asset/<id> -o <any-path>` (or a `>` redirect) would ALSO auto-match, letting a nominally read-only grant write arbitrary file contents into the repo — directly contradicting `analyst`'s design (100% read-only + MCP tools, "never writes source code"). The docs' suggested alternative (`WebFetch(domain:...)`) doesn't solve the underlying problem either, since WebFetch returns a model-paraphrased summary, not raw bytes — the exact deficiency skill §9 already documents as disqualifying for this use case. **Decision (user-directed, this session): defer, do not implement.** No changes to `.claude/settings.json`, `.claude/agents/analyst.md`, or design-extraction skill §9. T36 remains open in the external registry (`SESSIONS_OPEN_TAILS.md`, outside this repo, not edited by the agent this session) — this entry records, for this repo's own continuity, that the curl-pattern alternative was evaluated and rejected for the same injection-class reason the original full-Bash-grant attempt was rejected. **(iii) Confirmed, no change — `/spec`/`design-fixes.md` gate-wiring symmetry.** Re-verified directly: `spec.md` is already wired to all four gates via its orchestrator-side step (unchanged); `design-fixes.md` has zero gate references, matching `FLOWFORGE_ENGINE_SPEC.md` §16's explicit, reasoned exclusion (it performs no fresh Figma extraction) — confirmed still correct, not silently extended. PR: `.claude/commands/build.md` (Icons/vector-assets bullet gains the Gate 3 mechanical-check clause) + `CLAUDE.md` (version line only) + this log entry. **Engine edit → freeze-counter stays 0/3** (already reset by the Ф3 FULL PILOT RESET; no additional cost).

- 2026-07-05 — **Engine v3.74→v3.75 — Token map maintenance gains a "Scaffold audit" bullet: additive token/font writes were leaving contradicted create-next-app scaffold defaults behind (session 14, controller-caught defect from the first real `/baseline` APPLY on the Components file).** That APPLY left three scaffold defects in `globals.css`/`layout.tsx` that directly contradicted already-confirmed project facts: the create-next-app `prefers-color-scheme: dark` toggle and its light-mode `:root` values survived untouched alongside the newly-added dark-theme tokens, even though the confirmed theme has no light-mode variant at all — the old scaffold pair silently wins at render time, so the new tokens coexisted unconnected rather than actually driving the page; and Geist Mono remained imported, wired to a CSS variable, and left in `@theme`, despite zero usages anywhere in `src/` and no confirmed role in the 100%-Poppins font census — a genuine Core-Web-Vitals/bundle-size violation (an unnecessarily loaded web font), not a cosmetic nit. **Root cause:** every command that writes tokens/fonts into `globals.css`/`layout.tsx` — `/baseline`'s APPLY, `/build`'s token/primary-font application step, and `/design-fixes`' token-VALUE-change mode — is additive by definition: each specifies ADDING new tokens/fonts, never auditing what's already sitting in those same files against facts already confirmed by the time of writing. All three would reproduce this same class of defect on every future project that runs any of them, not just this one run — fixing only the symptom in this one PR would leave the other two entry points (and any future run of `/baseline` itself) equally exposed. **Fix:** `CLAUDE.md` §11 "Token map maintenance (incremental)" gains a new "Scaffold audit" bullet, inserted as a pure addition immediately after the existing "Primary-font application (idempotent guard)" bullet — the single shared home, since all three commands write to the same files and the rule must not be restated three times and drift. It requires: if the confirmed theme has no light-mode variant, remove the inherited light/dark toggle and wire the base background/foreground variables directly to the confirmed tokens instead of leaving both pairs coexisting; if a font family has zero usages in `src/` and no confirmed role in the design source, remove its import/variable/theme entry entirely. Scope is explicitly bounded — this is not exhaustive scaffold-hunting on every run, only an audit of the SAME file(s) the command is already writing to. All three command files (`baseline.md`, `build.md`, `design-fixes.md`) gain a one-line pointer to the CLAUDE.md §11 bullet at their respective token/font-writing steps, rather than restating the rule. PR: `CLAUDE.md` (§11, new bullet + version line) + `.claude/commands/baseline.md` (one-line pointer in Apply) + `.claude/commands/build.md` (one-line pointer after Primary-font application) + `.claude/commands/design-fixes.md` (one-line pointer in the token-VALUE-change classification) + this log entry. **Engine edit → freeze-counter stays 0/3** (already reset by the Ф3 FULL PILOT RESET; no additional cost).

- 2026-07-05 — **Engine v3.73→v3.74 — primary-font auto-confirm, opacity-aware completeness gate, near-match Role/Component tagging + gate, all four gates consolidated to skill §14, `/spec` wired to the same gates (session 14, controller-directed package following the Components-file checkpoint correction pass).** Six changes, one package: (i) **primary-font auto-confirm on unanimous census** — recorded as decision #31 in `FLOWFORGE_CHAT_CONTEXT.md` §4 per the user's recollection of an earlier, out-of-project session (not independently verifiable via this project's own chat history, recorded now as current canon regardless of when first decided), and codified into `design-extraction` skill §8: a 100%-unanimous font census (one family, zero competitors) auto-confirms without a checkpoint click; a genuine split (2+ families) still requires explicit human confirmation. (ii) **The completeness gate's color-only regex missed opacity-bearing attributes** — the Instagram Press-state `fill-opacity="0.8"` miss (caught this session re-verifying the Components-file checkpoint) was invisible to the old `COLOR_PATTERN`, which matched hex/rgba only; `validate-checkpoint-completeness.sh` now also matches `fill-opacity="X"`, `stroke-opacity="X"`, and bare `opacity="X"`, using ordinary alternation + leftmost-match semantics (no lookbehind needed, so it stays portable to BSD/macOS grep) — verified with a synthetic test that a `fill-opacity="0.8"` present in the raw transcript but absent from the draft is correctly flagged, and that when present in both it passes clean. (iii) **A near-match design question (`#E68A01` vs `#E98C00`) was raised despite failing skill §4's own same-slot test** — rather than another prose refinement (the same probabilistic-execution ceiling already seen on label-correlation, §10.7), §4 now REQUIRES explicit `[Role: ..., Component: ...]` tagging on every near-match pair, and a new mechanical gate, `validate-checkpoint-near-match-scoping.sh`, cross-checks the two tags for internal consistency — if Role or Component differ, the pair fails the same-slot test and should never have been raised as a near-match (honest scope limit: the gate verifies tag _consistency_, not tag _truthfulness_). New script adapted to this machine's stock bash 3.2 (`/usr/bin/env bash` has no `mapfile`, introduced in bash 4.0) — a portable `while read` loop into an array replaces it, functionally identical; verified with two synthetic lines (same Role/Component → not flagged, different Role → flagged, exit 1) matching the spec exactly. (iv) **All four mechanical gates were consolidated from inline `baseline.md` sections into a single canonical home, skill §14** — fixing a single-home violation (the same discipline already applied to §7.3 and INV-4/INV-6); `baseline.md` now points to skill §14 instead of restating each gate's logic inline. (v) **`/spec` is now wired to the same four gates via a new orchestrator-side step** — the `analyst` subagent has no `Bash`, so the analyst writes its raw transcript + draft spec to scratchpad files, and the ORCHESTRATOR runs the skill-§14 gates against those files after the analyst returns, resolving the Bash-access constraint architecturally without touching the deferred analyst-Bash-grant decision (registry T34/T36) — applies whether or not `/baseline` has already run on the project, since `/spec` may be the first command run (skill §8 fallback path). (vi) **`design-fixes.md` was deliberately left unwired** — it doesn't perform fresh Figma extraction (it only processes already-recorded design-questions and designer replies), so none of the four gates have raw extraction data to check against; confirmed via `git status` that the file was untouched. PR: `.claude/skills/design-extraction/SKILL.md` (§4 tagging rule + §8 auto-confirm bullet + new §14) + `.claude/scripts/validate-checkpoint-completeness.sh` (regex extension) + `.claude/scripts/validate-checkpoint-near-match-scoping.sh` (new file) + `.claude/commands/baseline.md` (three inline gate sections → one pointer to skill §14) + `.claude/commands/spec.md` (new orchestrator-gate subsection) + `CLAUDE.md` (version line only) + `FLOWFORGE_CHAT_CONTEXT.md` (handoff doc, versioned separately, not part of this PR's diff) + this log entry. **Engine edit → freeze-counter stays 0/3** (already reset by the Ф3 FULL PILOT RESET; no additional cost).

- 2026-07-05 — **Engine v3.72→v3.73 — third mechanical gate: flattened-node deep-read check (session 14, controller-directed generalization after the v3.71/v3.72 correction pass on the Components file).** The same-session `/baseline` correction pass that caught the Instagram Press-opacity, chevron-left gradient-stroke, and header-blur misses (v3.71/v3.72 entries) shared one common mechanical signature: in each case, `get_design_context` returned a flattened raster `<img>` reference for the node, and the value was accepted from that flattened result (or silently dropped) without ever calling `download_assets` to read the real SVG source — despite skill §9 already stating this exact "raster ≠ unavailable" rule in prose. Two prior mechanical gates (citation, completeness) don't catch this class: a citation gate only checks that a WRITTEN value has a node-id; a completeness gate only checks that an EXTRACTED value made it into the draft — neither checks whether the extraction that produced (or failed to produce) a value actually went deep enough. **Fix:** a third mechanical gate, `validate-checkpoint-deep-read.sh`, scans the raw transcript for every node-id whose `get_design_context` response contained a raster `<img>` reference and cross-checks it against every node-id ever passed to `download_assets` in the same transcript — flagging any node that was flattened but never re-verified via the deeper tool, regardless of what KIND of styled value would have been found there (generalizes beyond the specific color/opacity/blur instances that motivated it). Wired into `baseline.md` only, alongside the other two mechanical gates — NOT `spec.md`, since the `analyst` sub-agent `/spec` dispatches to has no `Bash` tool to run any of the three shell-script gates (same architectural constraint already documented for the existing two gates; a Bash grant for `analyst` remains a separate, deferred decision — registry T34/T36). PR: `.claude/scripts/validate-checkpoint-deep-read.sh` (new file) + `.claude/commands/baseline.md` (pure insertion) + `CLAUDE.md` (version line only) + this log entry. **Engine edit → freeze-counter stays 0/3** (already reset by the Ф3 FULL PILOT RESET; no additional cost).

- 2026-07-05 — **Engine v3.71→v3.72 — §9 gains a lead-in paragraph: icon STYLED PROPERTIES are extracted now, never deferred to /build or to a ticket (session 14, controller-caught defect in the same session's `/baseline` correction pass on the Components file).** The chevron-left icon's state-based gradient strokes (`Gradient/Hover`, `Gradient/Press`) were initially skipped during the main `/baseline` sweep as "just a decorative icon asset" — treated as if an icon's color, like its path geometry, could legitimately wait for a later per-ticket read. It was only caught by a second, controller-prompted correction pass re-reading this file's own prior history, not by either mechanical gate (citation/completeness), since the value had never entered the draft to be checked against. **Root cause:** §9 opens by stating icons are "vector geometry, not tokens," which is true of PATH GEOMETRY but was being read — by the executing pass, not licensed by any actual rule — as covering the icon's STYLED PROPERTIES (fill/stroke color, opacity, gradient, blur) too, exempting them from §1's "extract every styled property, the list is a structure not a filter" mandate. Geometry and color are different extraction problems with different legitimate deferral rules (a broad sweep may reasonably defer a full geometry pull across many icons; a per-state color is a design token like any border or fill, and has no such exception). §9 never said this explicitly, leaving the boundary to be inferred, and it was inferred wrong. **Fix:** `.claude/skills/design-extraction/SKILL.md` §9 gains a new lead-in paragraph (pure insertion, immediately after the existing "extract the real source, never invent it" sentence, before the first `download_assets` bullet — that bullet and everything after it left byte-for-byte intact) stating plainly: icon styled properties are extracted in whichever pass is currently reading the icon, with no "defer to /build" or "defer to the ticket" exception; only raw path geometry (`d=`, `viewBox`) may legitimately wait for a per-ticket read during a broad multi-icon sweep, and even then the icon's color is extracted now, not deferred alongside the geometry. PR: `.claude/skills/design-extraction/SKILL.md` (§9, lead-in paragraph, pure insertion) + `CLAUDE.md` (version line only) + this log entry. **Engine edit → freeze-counter stays 0/3** (already reset by the Ф3 FULL PILOT RESET; no additional cost).

- 2026-07-05 — **Engine v3.70→v3.71 — hard rule 2 generalized to every checkpoint command + a mechanical diff test replacing a materiality judgment call (session 14, controller-caught process gap during the same session's `/baseline` APPLY on the Components file).** During the first `/baseline` APPLY on the Components file, a post-approval, pre-write self-check found three real corrections against the checkpoint the controller had already approved: the Instagram icon's Press-state opacity changed from 100% (as approved) to 80% (`fill-opacity="0.8"`, direct SVG re-verification); new chevron-left icon gradient-stroke state tokens (`Gradient/Hover`, `Gradient/Press` reused as stroke gradients) were added that had NOT been part of the approved checkpoint at all; and the header glow-line blur radius was resolved from an approved "pending, not exposed by any tool" status to two concrete values (`feGaussianBlur stdDeviation="1"` plus a distinct `backdrop-filter:blur(10px)`/Figma-badge-`20` backdrop blur). All three were disclosed in the chat and in the resulting commit/log text, but were folded into the write BEFORE a second explicit approval — a process gap, not a content gap: the corrections were independently sound (confirmed correct on review), but hard rule 2's STOP had been treated as conditional on the executing agent's own assessment of whether a discrepancy was "material enough" to warrant re-approval, which is exactly the judgment call the checkpoint mechanism exists to remove from the agent's hands. Self-diagnosed as a deliberate-but-wrong call, not an oversight — the agent explicitly reasoned "these are corrections in service of the same approved intent, not new decisions" and chose disclosure-without-a-second-STOP on that basis. **Controller review, same session:** the three corrections were reviewed and explicitly approved after the fact; the commit was NOT reverted (the content was independently verified sound — reverting and re-committing byte-identical content would have been process theater, not a substantive fix), but the PROCESS gap is fixed going forward, not merely apologized for. **Fix:** hard rule 2 rewritten in full — (1) generalized from `/spec`-only to every command that produces a CHECKPOINT/STOP for approval (`/spec`, `/baseline`, `/tickets`, `/design-fixes`, and any future command with the same pattern); (2) a MECHANICAL diff test replaces the materiality judgment call: diff the about-to-be-written content against the exact approved text — proceeding without a new STOP is safe ONLY if the diff is empty except for added/strengthened citations on an otherwise-unchanged value; any changed value (even a "more correct" one), any newly-introduced node/component/token/usage not in the approved text, or any classification change (pending↔resolved, excluded↔included, question↔non-question) requires presenting the delta as its own mini-checkpoint and stopping again, regardless of direction or how minor it feels. PR: `CLAUDE.md` (hard rule 2, full replacement + version line only) + this log entry. **Engine edit → freeze-counter stays 0/3** (already reset by the Ф3 FULL PILOT RESET; no additional cost).

- 2026-07-05 — **Engine v3.69→v3.70 — §10 gains a lead-in paragraph on visual structure recognition, after three same-day rewrites of point 7 all missed the same root cause (session 13).** Point 7 was rewritten three times today (v3.59: "adjacent state-label sweep" formula → v3.60/v3.61: same-day correction, small-vs-growing-offset distance criteria → v3.62/v3.63: full replacement, reasoning-based grouping model). All three treated label-to-instance correlation as a computation problem to be solved FROM coordinates and text — distance formulas, offset consistency, structural-grouping reasoning — when a component/style-guide file's actual organizing principle is simpler and more direct: it exists specifically to show each element category's state set side-by-side, visually recognizable by looking at the whole panel, the way a human would. None of the three prior fixes instructed reading a screenshot of the whole panel FIRST, before any per-node position/text analysis — all three jumped straight to per-node computation. **Fix:** `.claude/skills/design-extraction/SKILL.md` §10 gains a new lead-in paragraph (pure insertion, immediately after the `## 10.` header and before the existing first sentence — NOT a new numbered point, since points 1–8 are cited elsewhere by number, e.g. "§10 point 7", and must keep their current numbers unchanged) requiring visual recognition of a showcase panel's structure — how many component-category panels, what state-columns each demonstrates — via a full screenshot of the relevant frame(s), BEFORE any per-node extraction. Applies equally to `/baseline` (reading a whole design-system file) and `/spec` (reading one component's state set for a ticket) — both load this skill, and the principle isn't baseline-specific: `/baseline` may not have run yet on a given project, or may run partway through one, so `/spec` needs the same recognition step independently. PR: `.claude/skills/design-extraction/SKILL.md` (§10, lead-in paragraph, pure insertion) + `CLAUDE.md` (version line only) + this log entry. **Engine edit → freeze-counter stays 0/3** (already reset by the Ф3 FULL PILOT RESET; no additional cost).

- 2026-07-05 — **Engine v3.68→v3.69 — §10 gains point 8, decorative-vector coverage below sweep granularity (session 13, registry T37 recurrence on the Components file).** T37 (a decorative-vector node extracted and tokenized in an earlier `/baseline` run on the Components file, then silently absent from a later run on the same file — a gap neither the citation gate nor the completeness gate landed in v3.68 can catch by construction, since the value never entered that run's raw transcript at all) **recurred a second time on the same specific node** — the header-divider vector — in the very next fresh `/baseline` run after being found and filed. **Root cause:** a parent-level "swept" marking in the page inventory (e.g. "Header — swept") does not guarantee every descendant decorative child was individually queried — a coverage gap one level below the granularity the existing inventory tracks; the parent-sweep marking was being read (by the executing analyst pass, not by any rule that sanctioned it) as covering its decorative children by implication. **Fix:** `.claude/skills/design-extraction/SKILL.md` §10 gains a new point 8 (pure insertion, between the end of point 7's closing sentence and the "Only against this attached inventory" closing paragraph — both left byte-for-byte intact), requiring explicit per-child `get_design_context`/`download_assets svg` verification of decorative vectors nested inside already-swept parents, and requiring a cross-check against any PRIOR approved checkpoint for the same file before treating a previously-tokenized element as out of scope on a re-run. **Scope boundary (explicit, not overreached):** this is a procedural requirement (check the child directly, check against prior canon), not a new mechanical gate — no tooling change, relies on the same discipline already established for the other §10 points; it does not by itself prove the recurrence is prevented. **T37 stays OPEN in the registry** pending a fresh blind `/baseline` run confirming this fix actually prevents recurrence — not closed on the strength of this edit alone. PR: `.claude/skills/design-extraction/SKILL.md` (§10, new point 8, pure insertion) + `CLAUDE.md` (version line only) + this log entry. **Engine edit → freeze-counter stays 0/3** (already reset by the Ф3 FULL PILOT RESET; no additional cost).

- 2026-07-05 — **Engine v3.67→v3.68 — completeness gate for silently-dropped color values (session 12, controller-directed re-verification of a `/baseline` checkpoint on the Components file).** A `/baseline` checkpoint on the Components file (session 12) passed the citation gate (every written value had a node-id) but still silently omitted several real, independently re-verified color values from the final map: `rgba(255,255,255,0.6)` (input placeholder, accordion body text, price "/day" suffix), `rgba(255,255,255,0.8)` (MenuItem Variant3 text), and `rgba(255,255,255,0.03)` (input field background) — all three confirmed extracted via raw tool output but absent from that session's draft text. **Root cause:** the citation gate only checks lines that WERE written; it cannot catch a value that was extracted via a tool call but never written into the draft at all — a different failure mode from a missing citation. **Fix:** a second, symmetric mechanical gate — `validate-checkpoint-completeness.sh` — diffs every color value seen in the raw tool-call transcript for the run against the final draft text, and blocks presentation until every raw-seen value is either recorded or explicitly noted as an intentional exclusion. Requires a new raw-transcript logging step during extraction (without it, there's nothing for this gate to check against). **Scope boundary, explicit:** this gate catches a value that WAS extracted but dropped from the draft. It does NOT catch (and is not claimed to catch): (1) a value that's present in the draft under a reused token but missing a per-usage citation row (a citation-gate concern, already covered by the existing gate); (2) a component whose color was never extracted at all in the first place, so no raw value exists to diff against (an extraction-coverage gap — tracked separately, see registry T37); (3) whether a recorded value is itself correct. Two additional findings surfaced during the same day's testing, controller-verified across separate sessions (not reproducible from this session's own transcript, noted here for completeness of the day's record, not as claims this specific gate resolves): a decorative header-divider vector whose color was never extracted in one run (the T37 "never attempted" class), and a chevron icon press-state recolor confirmed via a direct Figma tool call in a separate session. Neither is fixed by this change. PR: `.claude/scripts/validate-checkpoint-completeness.sh` (new file) + `.claude/commands/baseline.md` (two insertions: logging bullet + gate section) + `CLAUDE.md` (version line only) + this log entry. **Engine edit → freeze-counter stays 0/3** (already reset by the Ф3 FULL PILOT RESET; no additional cost).

- 2026-07-03 — **Engine v3.66→v3.67 — mechanical citation gate for `/baseline` checkpoints (session 12, controller-directed re-verification of two live checkpoint items on the Components file).** Two `/baseline` checkpoints on the Components file (session 12) each contained an Instagram-icon opacity line that aggregated three individually-different node states ("×3, 40% opacity" / then corrected, but the underlying pattern recurred a third time across the day) into one unverified number, with no node-id citations — the exact gap the just-landed skill §2 "Citation completeness check" targets, but a PROSE rule proved insufficient to reliably prevent recurrence across independent runs (probabilistic LLM execution, not a deterministic program). **Fix:** a MECHANICAL gate — new script `.claude/scripts/validate-checkpoint-citations.sh` (regex-checks that every design-value signal — hex color / percentage / px — appearing on a checkpoint line is accompanied by a Figma node-id pattern on the same line) wired into `baseline.md` immediately before the checkpoint is presented to the human. Exit non-zero blocks presentation until every flagged line is re-verified against the real Figma source and corrected. **Explicit scope boundary (documented, not silently overreached):** this catches MISSING citations, not WRONG values under a present citation — a citation's correctness still depends on the underlying tool call being accurate. PR: `.claude/scripts/validate-checkpoint-citations.sh` (new file) + `.claude/commands/baseline.md` (pure insertion) + `CLAUDE.md` (version line only) + this log entry. **Engine edit → freeze-counter stays 0/3** (already reset by the Ф3 FULL PILOT RESET; no additional cost).

- 2026-07-03 — **Engine v3.65→v3.66 — §2 gains a citation-completeness check (session 12, controller re-verification of a pending `/baseline` checkpoint on the Components file).** A live `/baseline` checkpoint's OTHER-catch-all section reported "Instagram icon — same icon, 3 opacity levels: Normal 40%, Hover 100%, Press 80%" with no node-id citations, unlike every other line in the report. The controller flagged the gap and asked for an individual re-check of all three instances (`2:117`/`2:126`/`2:134`) plus the header backdrop-blur claim. Re-verification confirmed the Press opacity value (80%) was in fact correct — but the missing citation meant that, at review time, there was no way to distinguish an independently re-checked number from one that merely happened to be right; the same gap would have hidden a wrong value just as easily. **Fix:** `.claude/skills/design-extraction/SKILL.md` §2 ("Census as aggregates only") gains a new "Citation completeness check" paragraph, inserted immediately after the existing Visibility-policy paragraph (pure insertion, that paragraph otherwise untouched): before finalizing any section, verify every reported value carries node-id citations per the section's own format; an uncited line is a signal the value wasn't independently re-checked and should be re-verified before the checkpoint is presented. PR: `.claude/skills/design-extraction/SKILL.md` (§2, pure insertion) + `CLAUDE.md` (version line only) + this log entry. **Engine edit → freeze-counter stays 0/3** (already reset by the Ф3 FULL PILOT RESET; no additional cost).

- 2026-07-03 — **Engine v3.64→v3.65 — WebFetch-vs-raw-bytes rule + "unexplained" self-audit rule; analyst.md Bash grant attempted and blocked (session 11, controller Q&A on a pending `/baseline` checkpoint for the Components file).** Controller review of the checkpoint surfaced two gaps. **(1) Fetch-capability mismatch across run paths:** the `analyst` sub-agent (used by `/spec`) has no `Bash`/`WebFetch` in its tool list at all, so some decorative-vector paint values (e.g. the Menu Item underline's gradient color) are genuinely unresolvable within its toolset once `download_assets` + `get_design_context` are exhausted — a real, structural constraint, not an oversight. By contrast, the just-completed `/baseline` run doesn't dispatch `analyst` — it ran entirely in the orchestrating session, which DOES have `Bash`/`WebFetch` — but that session used `WebFetch`'s paraphrased summary as sufficient evidence for a token value (the underline color) instead of following up with `curl` on the same already-returned export URL for raw bytes, only doing so reactively when challenged. **(2) Missed state recognition:** an Instagram icon's opacity-based Normal/Hover/Press pattern (`2:117`/`2:126`/`2:134`) sat in the exact same three-column grouping as adjacent Button/outline-button/chevron instances already correctly resolved by column position earlier in the same sweep, but was written up as an "unexplained inconsistency" design question instead of a recognized state — the correlating data was already on hand and simply wasn't cross-checked. **Fix landed (2 of 3 parts):** `.claude/skills/design-extraction/SKILL.md` §9 (decorative-vectors subsection) gains a new paragraph: any context with `Bash`/`WebFetch` available (orchestrator-run commands like `/baseline`/`/design-fixes`, which don't dispatch a tool-restricted sub-agent) must resolve to raw `curl` bytes on the returned export URL before recording a decorative-vector color as confirmed — a `WebFetch` paraphrase is a hint, not evidence; a context without Bash (`analyst` under `/spec`) stays bound by the existing STOP-and-ask rule, unchanged. §10 point 7 gains a new "Self-audit before writing 'unexplained'" paragraph: cross-check any property difference against every column/grouping already established in the same sweep before writing it up as unexplained or opening a design question — a difference coinciding with an already-identified grouping is a state finding, not a mystery, and skipping this check is a defect of the same severity as an unbacked absence claim. **Fix NOT landed — deliberately deferred, not a silent gap:** the planned third part, `.claude/agents/analyst.md` gaining a narrowly-scoped `Bash` grant (curl-only, on already-returned `download_assets` URLs, no other use, via a new "Restricted Bash use" section), was **blocked by the permission classifier** on the grounds that the "curl-only" restriction would be unenforced prose, not an actual technical tool-scope limit — granting `Bash` to `analyst` is a genuine widening of an otherwise strictly read-only, MCP-only sub-agent, not a paperwork step, and warrants human review outside auto mode. The attempted edit was reverted; `analyst.md` is byte-for-byte unchanged by this entry. Per human decision (asked directly, this session), this PR proceeds with only the two skill-level fixes; the `analyst`/`Bash` gap stays open and tracked here, not silently dropped — `analyst` continues to STOP-and-ask (existing skill §9 rule) on any decorative-vector paint value it can't resolve via its current MCP-only toolset. PR: `.claude/skills/design-extraction/SKILL.md` (§9 + §10 point 7, both pure insertions) + `CLAUDE.md` (version line only) + this log entry. **Not in this PR:** `.claude/agents/analyst.md` (edit attempted, blocked, reverted — see above; a future PR would need an explicit, human-approved decision on how to scope `Bash` for a sub-agent, since prose-only restriction is not sufficient). **Engine edit → freeze-counter stays 0/3** (already reset by the Ф3 FULL PILOT RESET; no additional cost).

- 2026-07-03 — **Engine v3.63→v3.64 — §4 near-match rule scoped to "same slot" (session 11, live `/baseline` finding on the Components file).** A live `/baseline` checkpoint on the Components Figma file raised two near-match design questions that added no real value: **(1)** "error" (`#ff5c00`) vs "primary" (`#e98c00`) — already distinct, independently-named semantic roles that happen to share a hue family; **(2)** a 1px text-shadow-blur difference between the filled button's shadow and the outline button's shadow — two different components' shadows, not one decision drifting. **Root cause:** §4's near-match rule triggered on visual/numeric proximity alone, with no test for whether the two values actually occupy the same decision-slot — over-flagging genuinely separate, already-justified decisions as if they were one drifting decision, which would clutter `docs/design-questions.md` with non-questions and dilute real ones. **Fix:** §4 near-match rule scoped with an explicit "same slot" test, inserted between the existing "Near-match" and "Match with a previously REMOVED value" bullets (pure insertion, both bullets otherwise untouched) — same named role + same component (or same property across states of the literally same component) required to trigger a question; different roles or different components/variants are recorded as independent tokens with no question, regardless of numeric/visual proximity. PR: `.claude/skills/design-extraction/SKILL.md` (§4, insertion only) + `CLAUDE.md` (version line only) + this log entry. **Engine edit → freeze-counter stays 0/3** (already reset by the Ф3 FULL PILOT RESET; no additional cost).

- 2026-07-03 — **Engine v3.62→v3.63 — §10 point 7 rewritten a third time, same day (session 11, INV-8 append-only — this is a further correction, not a rewrite, of the two entries below).** Point 7 (added earlier today as v3.59→v3.60, then corrected once today as v3.60→v3.61) proved insufficient a SECOND time on the same day's live `/baseline` testing — this time the gap was architectural, not a formula bug: a coordinate-distance/growing-offset formula cannot represent a real pattern found in the file, where ONE state-label triplet ("Normal/Hover/Press") governs a WHOLE COLUMN of stacked, unrelated component types — a button, a text link, an outline button, a social icon, and a loose chevron icon all sharing one heading — confirmed by direct Figma verification (chevron instance 2:145 carries an internal "Gradient/Press" style annotation matching its column's Press label; 2:143 carries none matching Normal). **Root cause:** the prior two formula-based versions of point 7 both assumed a one-label-to-one-owner model and tried to fix the DISTANCE MATH — version 1 (v3.60) required exact/small-consistent-offset matching; version 2 (v3.61) added large/growing-offset as evidence against a match. Neither version's underlying model could represent a label governing a variable-length group of heterogeneous components, which is architecturally different from a distance-tuning problem. **Fix:** point 7 rewritten a third time, this time replacing the positional-formula model entirely with a REASONING-based model — weigh structural grouping + Figma style-name evidence + plausibility together, explicitly permit three outcomes (single owner, group of owners, or no owner) without forcing any of them, and follow an evident grouping to its natural end rather than a fixed distance. PR: `.claude/skills/design-extraction/SKILL.md` (§10 point 7, full replacement) + `CLAUDE.md` (version line only) + this log entry. **Engine edit → freeze-counter stays 0/3** (already reset by the Ф3 FULL PILOT RESET; no additional cost).

- 2026-07-03 — **Engine v3.61→v3.62 — /spec fallback for primary-font determination when no /baseline has run (session 11, found via controller review of the actual command files, not assumed from the CLAUDE.md summary text).** **Gap found:** CLAUDE.md's law text states the primary-font application guard "fires only when a UI source exists... pure-logic work does not trigger it," implying the mechanism is complete — but a direct read of the actual command files showed neither `/spec.md` nor `/build.md` had any code path for the case where `/spec` runs on a project BEFORE `/baseline` has ever confirmed a primary font: `design-extraction` skill §8 stated flatly "`/spec` NEVER determines the primary font," `spec.md` had no fallback logic at all, and `build.md` had NO primary-font application step whatsoever (searched — genuinely absent, not merely unlinked). Since `/baseline` is optional (a project may go straight to `/spec`), this left primary-font determination completely unreachable on any project that skips `/baseline` first. **Fix — three coordinated files:** (1) `.claude/skills/design-extraction/SKILL.md` §8 fully rewritten from a single baseline-only path to a two-path model — preferred full-coverage census via `/baseline` (unchanged), plus a new fallback path where `/spec` determines a NARROW-SOURCE candidate from only its own ticket's frame(s) when `docs/design-tokens.md` has no confirmed value yet, confirmed at the normal spec-approval checkpoint (not a separate gate); once ANY value is confirmed by either path, all later `/spec` runs consume it, and a later full-coverage `/baseline` superseding a narrow-source value is treated as "canon moved" (RE-RUN delta mode), not a silent conflict. (2) `.claude/commands/spec.md` gains a new "Primary-font fallback check" subsection (inserted after the Reuses-dependency gate, before "## Do") wiring the skill §8 fallback path into the command. (3) `.claude/commands/build.md` gains the previously-missing "Primary-font application" bullet (inserted after the token-additions bullet, before "Animations") — idempotent `next/font`-on-`<body>` wiring, firing only when the ticket has a Figma source and a confirmed value exists (from either path), and writing a fresh fallback-sourced value to `docs/design-tokens.md` in the same commit if it wasn't already recorded. PR: `.claude/skills/design-extraction/SKILL.md` (§8 full replacement) + `.claude/commands/spec.md` (new subsection) + `.claude/commands/build.md` (new bullet) + `CLAUDE.md` (version line only) + this log entry. **Engine edit → freeze-counter stays 0/3** (already reset by the Ф3 FULL PILOT RESET; no additional cost).

- 2026-07-03 — **Engine v3.60→v3.61 — same-day correction to §10 point 7 (session 11, INV-8 append-only — this is a correction, not a rewrite, of the entry below).** The point 7 added earlier today (v3.59→v3.60, "adjacent state-label sweep") proved insufficient on its own first real `/baseline` run. What happened: a coverage sweep of the parent frame surfaced three unassigned "Normal"/"Hover"/"Active" text labels. The analyst correctly matched them to the Accordion component (an exact-x-coordinate match — the easy case) but then stopped, and defaulted a SECOND component with unresolved state semantics in the same sweep — Menu Item (four variants named only `Property 1=False/True/Variant3/Variant4`, no semantic label) — to a false "no adjacent label found" absence claim. Independent controller verification (computing each nested child's ABSOLUTE position — parent-frame offset + local offset, not the raw reported local x/y — and comparing offset consistency across the instance set) showed this was very likely wrong: Menu Item had never actually been exhaustively checked against the same label set: the original pass treated the first plausible match (Accordion) as clearing the whole sweep, not as clearing only that one component. **Root cause, not a new defect class:** the original point 7 said "sweep for labels" but didn't require correlating EVERY unassigned label against EVERY candidate instance, and didn't say what counts as match evidence when the coordinate match isn't exact (small consistent offsets, e.g. a label sitting 15–30px to one side and 50–65px above its instance, repeated across a full instance set) versus what counts as evidence AGAINST a match (a large or growing offset across instances). **Fix:** §10 point 7 rewritten in place (full replacement of the same not-yet-committed point, not a new point) to require: (a) absolute-position computation for nested children (sum parent + local offset, never read local coordinates in isolation), (b) checking ALL unassigned labels against ALL candidate instances in the same pass — a confirmed match on one component is not license to stop checking others, (c) explicit match/non-match evidence criteria (small consistent offset = valid match evidence; large or growing offset = evidence against). PR: `.claude/skills/design-extraction/SKILL.md` (§10 point 7, full replacement) + `CLAUDE.md` (version line only) + this log entry (appended above, not replacing, the v3.59→v3.60 entry below — INV-8). **Not in scope of this edit:** no re-analysis of Menu Item or any other specific component was performed as part of this task — that remains open, to be resolved separately under the corrected point 7. **Engine edit → freeze-counter stays 0/3** (already reset by the Ф3 FULL PILOT RESET; no additional cost).

- 2026-07-03 — **Engine v3.59→v3.60 — design-extraction skill hardening (session 11, live `/baseline` findings on the "Components" Figma file):** two pure-insertion additions to `.claude/skills/design-extraction/SKILL.md`, no existing content altered. **(1) §10 point 7 — adjacent state-label sweep:** before declaring a component's variant/state axis "unlabeled" or opening a design question about ambiguous styling semantics, the analyst must first sweep the immediate parent frame via `get_metadata` (not just the originally-requested child nodes) for sibling TEXT nodes — state labels (Normal/Hover/Press/Active) are frequently placed adjacent to, not inside, the instances they describe. Source: a live `/baseline` run initially flagged the Accordion component's border-color axis as an unlabeled/ambiguous design question; a human-directed re-sweep of the parent frame found three adjacent "Normal"/"Hover"/"Active" text labels (confirmed via `get_design_context`) that resolved it as a real, labeled state pattern — the original analyst pass had the data in an already-fetched parent-frame read but didn't check it. **(2) New §13 — extraction concurrency & rate-limit handling:** documents that the Figma MCP enforces an account/seat-level tool-call quota (per-day and per-minute), and that a 6-way parallel extraction fan-out exhausted it within a few calls during the same `/baseline` run, leaving 3 of 9 component groups unswept mid-checkpoint. Codifies: extract sequentially or with ≤2 concurrent sub-agents; on a rate-limit error, stop that sub-task immediately, report what was gathered vs. missing as a **tool-access gap** (never a design absence, per §10), and never retry in a loop. Re-running the same 3 groups sequentially (no parallelism) completed cleanly with zero rate-limit hits, confirming the fix. PR: `.claude/skills/design-extraction/SKILL.md` (§10 point 7 + new §13) + `CLAUDE.md` (version line only) + this log entry. **Engine edit → freeze-counter stays 0/3 (already reset by the Ф3 FULL PILOT RESET; no additional cost — no clean run was in progress to interrupt).**

- 2026-07-02 — **Engine v3.58→v3.59 — Ф3 engine overhaul (FULL PILOT RESET, decision #30, spec-first from `FLOWFORGE_ENGINE_SPEC.md` v1.4):** implements the ten-item revision (a–j) designed after the T25/T30 source-exhaustion defect class (analyst settling for the primary read and making false absence claims across FF-10/FF-18/FF-22). `.claude/agents/analyst.md` — tool grant (`mcp__figma__download_assets`, `mcp__figma__get_motion_context`, T29), a new mandatory **Coverage inventory** spec section backed by an extended `design-extraction` skill §10 (T30: enumerate ALL top-level nodes of ALL types — frames/groups/sections/canvases, not frames only — with type/name/size, no width-anchoring, before any absence claim; escalated from advisory to a mandatory ATTACHED ARTIFACT), a new hard rule for the intent-vs-ambiguity distinction (build per the mockup as observed + raise the design question in parallel, never in place of a section — an undecided intent never blocks; a genuinely unreachable datum still STOPs, unchanged), and citations to two new skill additions: `behaviour-library-map.md` (T28 — new companion file, seeded carousel→Swiper / continuous marquee→react-fast-marquee / accordion→Radix UI Accordion; expansion under human confirm, never an unrecorded ad-hoc pick) and `SKILL.md` §12 "Breakpoint continuity" (classify every cross-mockup difference as CONTINUOUS or DISCRETE when two or more same-pattern mockups are read). `.claude/settings.json` — `mcp__figma__get_motion_context` added to the Figma allow-list (`download_assets` was already present — the T29 gap was the analyst's own tool grant, not this list). `.claude/commands/spec.md` — new Reuses-dependency gate (spec-threshold: a Reused ticket's spec must exist, need not be built/approved) + fixed the stale T2Z parenthetical claiming the Jira comment needs a pre-post confirm (the live `jira-transition-guard.sh` hook already auto-allows `addCommentToJiraIssue` as class A — confirmed by direct read; the law was already correct, only this command-file line had drifted). `.claude/commands/build.md` — the matching Reuses-gate build-threshold (a Reused ticket must be BUILT — merged on `dev`) + a fluid-CSS-by-default mandate (`clamp()`/`min()`/`max()`/`aspect-ratio`/container queries for continuous properties across the full range; discrete layout-mode switches only at the spec's named threshold; never two fixed `@media` states with the gap unengineered) + a behaviour-library-map consultation step for derived-behaviour implementation picks. `CLAUDE.md` §11 — the `/spec` and `/build` definitions gain the matching Reuses-gate clauses, the Coverage-inventory/breakpoint-classification mandates, and the fluid-CSS default; the "Clean-run & freeze-counter" section gains **(L1.5)** as a new review-time criterion (same nature as the existing L2): Clause A — an absence claim with no attached coverage inventory is a defect; Clause B — an unclassified cross-mockup difference, on any ticket where two-or-more same-pattern mockups were read, is a defect of the same severity. Source: spec-first (`FLOWFORGE_ENGINE_SPEC.md` v1.4 / decision #30 / registry T28, T29, T30, plus items (d)/(f)/(j)). **Not in this PR:** the §4.6 analyst-model fork (Sonnet→Opus) — confirmed by direct grep that no `model:` field exists in any of the three subagent frontmatters, i.e. the T19 base model-routing hypothesis was never implemented as engine config in the first place, so there is nothing for this fork to change; it stays tracked in `FLOWFORGE_ENGINE_SPEC.md` §4.6 + the registry only, same precedent as T20/T21. Also flagged, not fixed (out of the ten-item scope): `mcp__figma__get_libraries` and `mcp__figma__search_design_system` are in the analyst's `tools:` list but not in this allow-list — a pre-existing friction gap, found during inventory, left for a future pass. PR: `chore/f3-engine-overhaul` → `dev` (squash); `.claude/agents/analyst.md` + `.claude/commands/{spec,build}.md` + `.claude/settings.json` + `.claude/skills/design-extraction/SKILL.md` + `.claude/skills/design-extraction/behaviour-library-map.md` (new) + `CLAUDE.md` (§11 + Clean-run section + version line) + this log entry. **Engine edit → freeze-counter stays 0/3 (already reset by the FULL PILOT RESET; no practical cost).**
